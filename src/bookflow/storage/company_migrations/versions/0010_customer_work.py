"""Frozen customer-work schema and scope CHECK extension; existing rows are preserved."""
from alembic import op

revision = 'co0010'
down_revision = 'co0009'
branch_labels = None
depends_on = None

# Literal revision-local DDL. Never import application metadata here.
DDL = (
    "CREATE TABLE _co0010_custom_field_scopes (\n\tid VARCHAR(26) NOT NULL, \n\tdefinition_id VARCHAR(26) NOT NULL, \n\tposition INTEGER NOT NULL, \n\tactive BOOLEAN NOT NULL, \n\trecord_type VARCHAR(32) NOT NULL, \n\tdefinition_name VARCHAR(200) NOT NULL, \n\tdefinition_name_key VARCHAR(400) NOT NULL, \n\tdefinition_active BOOLEAN NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_custom_field_defs_definition_id_position CHECK (position >= 0), \n\tCONSTRAINT ck_custom_field_scopes_record_type CHECK (record_type IN ('customer','vendor','employee','other_name','item','journal_entry','invoice','sales_receipt','credit_memo','payment','deposit','bill','bill_payment','check','credit_card_charge','transfer','inventory_adjustment','vendor_credit','proposal','work_order','estimate','sales_order','purchase_order','item_receipt','statement')), \n\tFOREIGN KEY(definition_id) REFERENCES custom_field_defs (id) ON DELETE RESTRICT\n)",
    'INSERT INTO _co0010_custom_field_scopes (id, definition_id, position, active, record_type, definition_name, definition_name_key, definition_active) SELECT id, definition_id, position, active, record_type, definition_name, definition_name_key, definition_active FROM custom_field_scopes',
    'DROP TABLE custom_field_scopes',
    'ALTER TABLE _co0010_custom_field_scopes RENAME TO custom_field_scopes',
    "CREATE TABLE work_documents (\n\tid VARCHAR(26) NOT NULL, \n\tversion INTEGER NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\tupdated_at VARCHAR(32) NOT NULL, \n\tupdated_by VARCHAR(26) NOT NULL, \n\tupdated_via VARCHAR(16) NOT NULL, \n\tkind VARCHAR(16) NOT NULL, \n\tnumber VARCHAR(64) NOT NULL, \n\tcurrent_revision_id VARCHAR(26) NOT NULL, \n\tstatus VARCHAR(16) NOT NULL, \n\tactive BOOLEAN NOT NULL, \n\testimate_group_id VARCHAR(26), \n\tPRIMARY KEY (id), \n\tCONSTRAINT uq_work_kind_number UNIQUE (kind, number), \n\tCONSTRAINT ck_work_kind CHECK (kind IN ('proposal','estimate','work_order')), \n\tCONSTRAINT ck_work_group CHECK ((kind = 'estimate' AND estimate_group_id IS NOT NULL) OR (kind <> 'estimate' AND estimate_group_id IS NULL)), \n\tCONSTRAINT ck_work_status CHECK ((kind IN ('proposal','estimate') AND status IN ('draft','open','accepted','declined','superseded','cancelled')) OR (kind = 'work_order' AND status IN ('draft','scheduled','in_progress','on_hold','complete','cancelled'))), \n\tCONSTRAINT ck_work_active CHECK (active IN (0,1)), \n\tCONSTRAINT ck_work_version CHECK (typeof(version) = 'integer' AND version > 0), \n\tCONSTRAINT ck_work_number CHECK (length(trim(number)) BETWEEN 1 AND 64), \n\tCONSTRAINT fk_work_current_revision FOREIGN KEY(id, current_revision_id) REFERENCES work_revisions (document_id, id) DEFERRABLE INITIALLY DEFERRED, \n\tFOREIGN KEY(estimate_group_id) REFERENCES work_documents (id)\n)",
    "CREATE UNIQUE INDEX uq_work_accepted_group ON work_documents (estimate_group_id) WHERE kind = 'estimate' AND status = 'accepted'",
    "CREATE TABLE work_revisions (\n\tid VARCHAR(26) NOT NULL, \n\tdocument_id VARCHAR(26) NOT NULL, \n\trevision_number BIGINT NOT NULL, \n\tsupersedes_revision_id VARCHAR(26), \n\tdate VARCHAR(10) NOT NULL, \n\tnumber VARCHAR(64) NOT NULL, \n\ttitle VARCHAR(200) NOT NULL, \n\tstatus VARCHAR(16) NOT NULL, \n\tactive BOOLEAN NOT NULL, \n\tcustomer_id VARCHAR(26) NOT NULL, \n\tcurrency VARCHAR(3) NOT NULL, \n\tnet_minor_units BIGINT NOT NULL, \n\ttax_minor_units BIGINT NOT NULL, \n\tgross_minor_units BIGINT NOT NULL, \n\taccepted_revision_id VARCHAR(26), \n\taccepted_at VARCHAR(32), \n\taccepted_by VARCHAR(26), \n\tdecision_note VARCHAR(2000), \n\tfacts_snapshot TEXT NOT NULL, \n\tcustom_fields_snapshot TEXT NOT NULL, \n\taudit_event_id VARCHAR(26) NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_work_facts_snapshot CHECK (json_valid(facts_snapshot) AND json_type(facts_snapshot) = 'object'), \n\tCONSTRAINT ck_work_custom_fields_snapshot CHECK (json_valid(custom_fields_snapshot) AND json_type(custom_fields_snapshot) = 'object'), \n\tCONSTRAINT uq_work_revision_owner UNIQUE (document_id, id), \n\tCONSTRAINT uq_work_revision_number UNIQUE (document_id, revision_number), \n\tCONSTRAINT fk_work_supersedes FOREIGN KEY(document_id, supersedes_revision_id) REFERENCES work_revisions (document_id, id), \n\tCONSTRAINT fk_work_acceptance FOREIGN KEY(document_id, accepted_revision_id) REFERENCES work_revisions (document_id, id) DEFERRABLE INITIALLY DEFERRED, \n\tCONSTRAINT ck_work_revision_number CHECK (typeof(revision_number) = 'integer' AND revision_number > 0), \n\tCONSTRAINT ck_work_net_minor_units CHECK (typeof(net_minor_units) = 'integer' AND net_minor_units >= 0), \n\tCONSTRAINT ck_work_tax_minor_units CHECK (typeof(tax_minor_units) = 'integer' AND tax_minor_units >= 0), \n\tCONSTRAINT ck_work_gross_minor_units CHECK (typeof(gross_minor_units) = 'integer' AND gross_minor_units >= 0), \n\tCONSTRAINT ck_work_total CHECK (gross_minor_units = net_minor_units + tax_minor_units), \n\tCONSTRAINT ck_work_revision_active CHECK (active IN (0,1)), \n\tCONSTRAINT ck_work_title CHECK (length(trim(title)) BETWEEN 1 AND 200), \n\tCONSTRAINT ck_work_revision_number_text CHECK (length(trim(number)) BETWEEN 1 AND 64), \n\tCONSTRAINT ck_work_currency CHECK (length(currency) = 3), \n\tCONSTRAINT ck_work_acceptance_evidence CHECK ((accepted_revision_id IS NULL AND accepted_at IS NULL AND accepted_by IS NULL AND status <> 'accepted') OR (accepted_revision_id IS NOT NULL AND accepted_at IS NOT NULL AND accepted_by IS NOT NULL AND status = 'accepted')), \n\tFOREIGN KEY(document_id) REFERENCES work_documents (id), \n\tFOREIGN KEY(customer_id) REFERENCES customers (id), \n\tFOREIGN KEY(audit_event_id) REFERENCES audit_events (id)\n)",
    'CREATE INDEX ix_work_revision_customer ON work_revisions (customer_id)',
    'CREATE INDEX ix_work_revision_query ON work_revisions (date, document_id)',
    'CREATE TABLE work_line_identities (\n\tid VARCHAR(26) NOT NULL, \n\tdocument_id VARCHAR(26) NOT NULL, \n\troot_document_id VARCHAR(26) NOT NULL, \n\troot_line_id VARCHAR(26) NOT NULL, \n\tsource_line_id VARCHAR(26), \n\tPRIMARY KEY (id), \n\tCONSTRAINT uq_work_identity_owner UNIQUE (document_id, id), \n\tCONSTRAINT fk_work_root FOREIGN KEY(root_document_id, root_line_id) REFERENCES work_line_identities (document_id, id) DEFERRABLE INITIALLY DEFERRED, \n\tFOREIGN KEY(document_id) REFERENCES work_documents (id), \n\tFOREIGN KEY(source_line_id) REFERENCES work_lines (id)\n)',
    "CREATE TABLE work_lines (\n\tid VARCHAR(26) NOT NULL, \n\tdocument_id VARCHAR(26) NOT NULL, \n\trevision_id VARCHAR(26) NOT NULL, \n\tline_id VARCHAR(26) NOT NULL, \n\tposition BIGINT NOT NULL, \n\titem_id VARCHAR(26) NOT NULL, \n\tunit_id VARCHAR(26), \n\tquantity_microunits BIGINT NOT NULL, \n\tcompleted_quantity_microunits BIGINT NOT NULL, \n\tunit_factor_nanounits BIGINT NOT NULL, \n\tbase_quantity_microunits BIGINT NOT NULL, \n\tunit_price_minor_units BIGINT, \n\tnet_minor_units BIGINT NOT NULL, \n\ttax_minor_units BIGINT NOT NULL, \n\tgross_minor_units BIGINT NOT NULL, \n\testimated_unit_cost_minor_units BIGINT, \n\testimated_cost_minor_units BIGINT, \n\tpricing_basis VARCHAR(16) NOT NULL, \n\tmarkup_percent_millionths BIGINT, \n\tbillable BOOLEAN NOT NULL, \n\tfacts_snapshot TEXT NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_work_facts_snapshot CHECK (json_valid(facts_snapshot) AND json_type(facts_snapshot) = 'object'), \n\tCONSTRAINT uq_work_line_owner UNIQUE (document_id, revision_id, id), \n\tCONSTRAINT uq_work_line_identity UNIQUE (revision_id, line_id), \n\tCONSTRAINT uq_work_line_position UNIQUE (revision_id, position), \n\tCONSTRAINT fk_work_line_revision FOREIGN KEY(document_id, revision_id) REFERENCES work_revisions (document_id, id), \n\tCONSTRAINT fk_work_line_identity FOREIGN KEY(document_id, line_id) REFERENCES work_line_identities (document_id, id), \n\tCONSTRAINT ck_work_position CHECK (typeof(position) = 'integer' AND position > 0), \n\tCONSTRAINT ck_work_quantity_microunits CHECK (typeof(quantity_microunits) = 'integer' AND quantity_microunits > 0), \n\tCONSTRAINT ck_work_unit_factor_nanounits CHECK (typeof(unit_factor_nanounits) = 'integer' AND unit_factor_nanounits > 0), \n\tCONSTRAINT ck_work_base_quantity_microunits CHECK (typeof(base_quantity_microunits) = 'integer' AND base_quantity_microunits > 0), \n\tCONSTRAINT ck_work_completed_quantity_microunits CHECK (typeof(completed_quantity_microunits) = 'integer' AND completed_quantity_microunits >= 0), \n\tCONSTRAINT ck_work_completed CHECK (completed_quantity_microunits <= quantity_microunits), \n\tCONSTRAINT ck_work_unit_price_minor_units CHECK (unit_price_minor_units IS NULL OR (typeof(unit_price_minor_units) = 'integer' AND unit_price_minor_units >= 0)), \n\tCONSTRAINT ck_work_estimated_unit_cost_minor_units CHECK (estimated_unit_cost_minor_units IS NULL OR (typeof(estimated_unit_cost_minor_units) = 'integer' AND estimated_unit_cost_minor_units >= 0)), \n\tCONSTRAINT ck_work_estimated_cost_minor_units CHECK (estimated_cost_minor_units IS NULL OR (typeof(estimated_cost_minor_units) = 'integer' AND estimated_cost_minor_units >= 0)), \n\tCONSTRAINT ck_work_net_minor_units CHECK (typeof(net_minor_units) = 'integer' AND net_minor_units >= 0), \n\tCONSTRAINT ck_work_tax_minor_units CHECK (typeof(tax_minor_units) = 'integer' AND tax_minor_units >= 0), \n\tCONSTRAINT ck_work_gross_minor_units CHECK (typeof(gross_minor_units) = 'integer' AND gross_minor_units >= 0), \n\tCONSTRAINT ck_work_total CHECK (gross_minor_units = net_minor_units + tax_minor_units), \n\tCONSTRAINT ck_work_billable CHECK (billable IN (0,1)), \n\tCONSTRAINT ck_work_known_cost CHECK ((estimated_unit_cost_minor_units IS NULL) = (estimated_cost_minor_units IS NULL)), \n\tCONSTRAINT ck_work_pricing CHECK ((pricing_basis = 'amount' AND unit_price_minor_units IS NULL AND markup_percent_millionths IS NULL) OR (pricing_basis IN ('catalog','manual') AND unit_price_minor_units IS NOT NULL AND markup_percent_millionths IS NULL) OR (pricing_basis = 'markup' AND unit_price_minor_units IS NOT NULL AND estimated_unit_cost_minor_units IS NOT NULL AND typeof(markup_percent_millionths) = 'integer' AND markup_percent_millionths BETWEEN -100000000 AND 1000000000000)), \n\tFOREIGN KEY(item_id) REFERENCES items (id), \n\tFOREIGN KEY(unit_id) REFERENCES unit_conversions (id)\n)",
    "CREATE TABLE work_links (\n\tid VARCHAR(26) NOT NULL, \n\tsource_document_id VARCHAR(26) NOT NULL, \n\tsource_revision_id VARCHAR(26) NOT NULL, \n\tdestination_document_id VARCHAR(26) NOT NULL, \n\tdestination_revision_id VARCHAR(26) NOT NULL, \n\trelation VARCHAR(24) NOT NULL, \n\tconversion_key_hash VARCHAR(64), \n\trequest_hash VARCHAR(64), \n\tsource_version BIGINT NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT uq_work_conversion_key UNIQUE (conversion_key_hash), \n\tCONSTRAINT uq_work_destination_birth UNIQUE (destination_document_id), \n\tCONSTRAINT fk_work_link_source FOREIGN KEY(source_document_id, source_revision_id) REFERENCES work_revisions (document_id, id), \n\tCONSTRAINT fk_work_link_destination FOREIGN KEY(destination_document_id, destination_revision_id) REFERENCES work_revisions (document_id, id), \n\tCONSTRAINT ck_work_source_version CHECK (typeof(source_version) = 'integer' AND source_version > 0), \n\tCONSTRAINT ck_work_link_distinct CHECK (source_document_id <> destination_document_id), \n\tCONSTRAINT ck_work_link_key CHECK ((relation = 'copy' AND conversion_key_hash IS NULL AND request_hash IS NULL) OR (relation IN ('proposal_estimate','estimate_work_order') AND conversion_key_hash IS NOT NULL AND request_hash IS NOT NULL AND length(conversion_key_hash) = 64 AND length(request_hash) = 64))\n)",
    "CREATE UNIQUE INDEX uq_work_estimate_order ON work_links (source_document_id) WHERE relation = 'estimate_work_order'",
    "CREATE TRIGGER work_revisions_immutable_update BEFORE UPDATE ON work_revisions BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_revisions_immutable_delete BEFORE DELETE ON work_revisions BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_line_identities_immutable_update BEFORE UPDATE ON work_line_identities BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_line_identities_immutable_delete BEFORE DELETE ON work_line_identities BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_lines_immutable_update BEFORE UPDATE ON work_lines BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_lines_immutable_delete BEFORE DELETE ON work_lines BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_links_immutable_update BEFORE UPDATE ON work_links BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_links_immutable_delete BEFORE DELETE ON work_links BEGIN SELECT RAISE(ABORT, 'immutable work history'); END",
    "CREATE TRIGGER work_group_insert BEFORE INSERT ON work_documents WHEN NEW.kind = 'estimate' AND NEW.estimate_group_id <> NEW.id AND NOT EXISTS (SELECT 1 FROM work_documents g WHERE g.id = NEW.estimate_group_id AND (g.kind = 'proposal' OR (g.kind = 'estimate' AND g.estimate_group_id = g.id))) BEGIN SELECT RAISE(ABORT, 'invalid estimate group owner'); END",
    "CREATE TRIGGER work_group_update BEFORE UPDATE ON work_documents WHEN NEW.kind = 'estimate' AND NEW.estimate_group_id <> NEW.id AND NOT EXISTS (SELECT 1 FROM work_documents g WHERE g.id = NEW.estimate_group_id AND (g.kind = 'proposal' OR (g.kind = 'estimate' AND g.estimate_group_id = g.id))) BEGIN SELECT RAISE(ABORT, 'invalid estimate group owner'); END",
    "CREATE TRIGGER work_document_identity BEFORE UPDATE ON work_documents WHEN NEW.id IS NOT OLD.id OR NEW.kind IS NOT OLD.kind OR NEW.estimate_group_id IS NOT OLD.estimate_group_id BEGIN SELECT RAISE(ABORT, 'immutable work document ownership'); END",
    "CREATE TRIGGER work_revision_kind BEFORE INSERT ON work_revisions WHEN NOT EXISTS (SELECT 1 FROM work_documents d WHERE d.id = NEW.document_id AND ((d.kind IN ('proposal','estimate') AND NEW.status IN ('draft','open','accepted','declined','superseded','cancelled')) OR (d.kind = 'work_order' AND NEW.status IN ('draft','scheduled','in_progress','on_hold','complete','cancelled')))) BEGIN SELECT RAISE(ABORT, 'invalid work revision state'); END",
    "CREATE TRIGGER work_identity_root BEFORE INSERT ON work_line_identities WHEN NOT ((NEW.root_document_id = NEW.document_id AND NEW.root_line_id = NEW.id) OR EXISTS (SELECT 1 FROM work_lines s JOIN work_line_identities i ON i.id = s.line_id JOIN work_documents d ON d.id = NEW.document_id JOIN work_documents o ON o.id = s.document_id WHERE s.id = NEW.source_line_id AND d.kind = 'work_order' AND o.kind = 'estimate' AND NEW.root_document_id = i.root_document_id AND NEW.root_line_id = i.root_line_id)) BEGIN SELECT RAISE(ABORT, 'invalid work billing root'); END",
    "CREATE TRIGGER work_identity_source BEFORE INSERT ON work_line_identities WHEN NEW.source_line_id IS NOT NULL AND EXISTS (SELECT 1 FROM work_lines s WHERE s.id = NEW.source_line_id AND s.document_id = NEW.document_id) BEGIN SELECT RAISE(ABORT, 'work source must belong to another document'); END",
    "CREATE TRIGGER work_line_completion_kind BEFORE INSERT ON work_lines WHEN NEW.completed_quantity_microunits <> 0 AND EXISTS (SELECT 1 FROM work_documents d WHERE d.id = NEW.document_id AND d.kind <> 'work_order') BEGIN SELECT RAISE(ABORT, 'completion belongs to work orders'); END",
    "CREATE TRIGGER work_link_relation BEFORE INSERT ON work_links WHEN NOT EXISTS (SELECT 1 FROM work_documents s JOIN work_documents d JOIN work_revisions r ON r.id = NEW.destination_revision_id WHERE s.id = NEW.source_document_id AND d.id = NEW.destination_document_id AND r.revision_number = 1 AND ((NEW.relation = 'copy' AND s.kind = d.kind AND (d.kind <> 'estimate' OR d.estimate_group_id IN (d.id,s.estimate_group_id))) OR (NEW.relation = 'proposal_estimate' AND s.kind = 'proposal' AND d.kind = 'estimate' AND d.estimate_group_id = s.id) OR (NEW.relation = 'estimate_work_order' AND s.kind = 'estimate' AND s.status = 'accepted' AND d.kind = 'work_order' AND EXISTS (SELECT 1 FROM work_revisions a WHERE a.id = NEW.source_revision_id AND a.status = 'accepted' AND a.accepted_revision_id IS NOT NULL)))) BEGIN SELECT RAISE(ABORT, 'invalid work conversion lineage'); END",
)


def upgrade() -> None:
    connection = op.get_bind()
    # As in co0009, SQLite RENAME validates dependent schema objects. Retain
    # all persistent views and triggers (including those on other tables),
    # and only the indexes belonging to the one rebuilt table. The migration
    # runner encloses this entire operation in its transaction with FKs off.
    retained = connection.exec_driver_sql(
        "SELECT type, name, sql FROM sqlite_schema WHERE sql IS NOT NULL AND ("
        "type IN ('view', 'trigger') OR (type = 'index' AND tbl_name = "
        "'custom_field_scopes')) "
        "ORDER BY CASE type WHEN 'view' THEN 0 WHEN 'index' THEN 1 ELSE 2 END, name"
    ).all()
    # INSTEAD OF triggers must be dropped before the views owning them.
    for kind in ('trigger', 'view'):
        for object_kind, name, _ in retained:
            if object_kind == kind:
                quoted = '"' + name.replace('"', '""') + '"'
                connection.exec_driver_sql(f'DROP {kind.upper()} main.{quoted}')
    for statement in DDL:
        op.execute(statement)
    for _, _, statement in retained:
        connection.exec_driver_sql(statement)
    if connection.exec_driver_sql('PRAGMA foreign_key_check').fetchone() is not None:
        raise RuntimeError('co0010 foreign key check failed')


def downgrade() -> None:
    raise NotImplementedError

"""Add durable complete payment recovery evidence without changing old objects."""
from alembic import op

revision = "co0018"
down_revision = "co0017"
branch_labels = depends_on = None

DDL = ("CREATE TABLE payment_selection_recoveries (\n\tid VARCHAR(26) NOT NULL, \n\tversion INTEGER NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\tupdated_at VARCHAR(32) NOT NULL, \n\tupdated_by VARCHAR(26) NOT NULL, \n\tupdated_via VARCHAR(16) NOT NULL, \n\tselection_id VARCHAR(26) NOT NULL, \n\trecovery_key VARCHAR(128) NOT NULL, \n\trequest_schema_version BIGINT NOT NULL, \n\tattempt_generation VARCHAR(36) NOT NULL, \n\tlocal_baseline_revision_id VARCHAR(26) NOT NULL, \n\tanchor_revision_id VARCHAR(26) NOT NULL, \n\tanchor_selection_version BIGINT NOT NULL, \n\theader_intent TEXT NOT NULL, \n\tdeclared_entry_count BIGINT NOT NULL, \n\tintent_hash VARCHAR(64) NOT NULL, \n\tstate VARCHAR(16) NOT NULL, \n\tapplied_revision_id VARCHAR(26), \n\tbegin_request_snapshot TEXT NOT NULL, \n\tbegin_receipt_snapshot TEXT NOT NULL, \n\tbegin_request_hash VARCHAR(64) NOT NULL, \n\tseal_request_snapshot TEXT, \n\tseal_receipt_snapshot TEXT, \n\tseal_request_hash VARCHAR(64), \n\tterminal_request_snapshot TEXT, \n\tterminal_receipt_snapshot TEXT, \n\tterminal_request_hash VARCHAR(64), \n\taudit_event_id VARCHAR(26) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_recovery_begin_request_snapshot CHECK (COALESCE((CASE WHEN json_valid(begin_request_snapshot) THEN json_type(begin_request_snapshot) = 'object' ELSE 0 END), 0)), \n\tCONSTRAINT ck_recovery_begin_receipt_snapshot CHECK (COALESCE((CASE WHEN json_valid(begin_receipt_snapshot) THEN json_type(begin_receipt_snapshot) = 'object' ELSE 0 END), 0)), \n\tCONSTRAINT ck_recovery_seal_request_snapshot CHECK (COALESCE((seal_request_snapshot IS NULL OR (CASE WHEN json_valid(seal_request_snapshot) THEN json_type(seal_request_snapshot) = 'object' ELSE 0 END)), 0)), \n\tCONSTRAINT ck_recovery_seal_receipt_snapshot CHECK (COALESCE((seal_receipt_snapshot IS NULL OR (CASE WHEN json_valid(seal_receipt_snapshot) THEN json_type(seal_receipt_snapshot) = 'object' ELSE 0 END)), 0)), \n\tCONSTRAINT ck_recovery_terminal_request_snapshot CHECK (COALESCE((terminal_request_snapshot IS NULL OR (CASE WHEN json_valid(terminal_request_snapshot) THEN json_type(terminal_request_snapshot) = 'object' ELSE 0 END)), 0)), \n\tCONSTRAINT ck_recovery_terminal_receipt_snapshot CHECK (COALESCE((terminal_receipt_snapshot IS NULL OR (CASE WHEN json_valid(terminal_receipt_snapshot) THEN json_type(terminal_receipt_snapshot) = 'object' ELSE 0 END)), 0)), \n\tCONSTRAINT ck_recovery_header_intent CHECK (COALESCE((CASE WHEN json_valid(header_intent) THEN json_type(header_intent) = 'object' ELSE 0 END), 0)), \n\tCONSTRAINT ck_recovery_declared_entry_count CHECK (COALESCE((typeof(declared_entry_count) = 'integer' AND declared_entry_count >= 0), 0)), \n\tCONSTRAINT ck_recovery_anchor_selection_version CHECK (COALESCE((typeof(anchor_selection_version) = 'integer' AND anchor_selection_version >= 1), 0)), \n\tCONSTRAINT ck_recovery_version CHECK (COALESCE((typeof(version) = 'integer' AND version >= 1), 0)), \n\tCONSTRAINT ck_recovery_format CHECK (COALESCE((typeof(request_schema_version) = 'integer' AND request_schema_version = 1), 0)), \n\tCONSTRAINT ck_recovery_state CHECK (COALESCE((state IN ('uploading','sealed','applied','aborted','superseded') AND ((state = 'applied') = (applied_revision_id IS NOT NULL))), 0)), \n\tCONSTRAINT ck_recovery_intent_hash CHECK (COALESCE((length(intent_hash)=64 AND intent_hash NOT GLOB '*[^0-9a-f]*'), 0)), \n\tCONSTRAINT ck_recovery_generation CHECK (COALESCE((length(attempt_generation)=36 AND substr(attempt_generation,9,1)='-' AND substr(attempt_generation,14,1)='-' AND substr(attempt_generation,19,1)='-' AND substr(attempt_generation,24,1)='-' AND replace(attempt_generation,'-','') NOT GLOB '*[^0-9a-f]*'), 0)), \n\tCONSTRAINT ck_recovery_terminal_receipt CHECK (COALESCE(((state IN ('applied','aborted','superseded')) = (terminal_receipt_snapshot IS NOT NULL) AND (terminal_receipt_snapshot IS NULL) = (terminal_request_snapshot IS NULL) AND (terminal_receipt_snapshot IS NULL) = (terminal_request_hash IS NULL)), 0)), \n\tCONSTRAINT ck_recovery_seal_receipt CHECK (COALESCE(((seal_receipt_snapshot IS NULL) = (seal_request_snapshot IS NULL) AND (seal_receipt_snapshot IS NULL) = (seal_request_hash IS NULL) AND (state <> 'uploading' OR seal_receipt_snapshot IS NULL) AND (state NOT IN ('sealed','applied') OR seal_receipt_snapshot IS NOT NULL)), 0)), \n\tUNIQUE (recovery_key), \n\tUNIQUE (selection_id, id), \n\tFOREIGN KEY(selection_id) REFERENCES payment_selections (id), \n\tFOREIGN KEY(selection_id, local_baseline_revision_id) REFERENCES payment_selection_revisions (selection_id, id), \n\tFOREIGN KEY(selection_id, anchor_revision_id) REFERENCES payment_selection_revisions (selection_id, id), \n\tFOREIGN KEY(selection_id, applied_revision_id) REFERENCES payment_selection_revisions (selection_id, id), \n\tFOREIGN KEY(audit_event_id) REFERENCES audit_events (id)\n)", 'CREATE INDEX ix_recovery_selection ON payment_selection_recoveries (selection_id, created_at, id)', "CREATE TABLE payment_selection_recovery_chunks (\n\tid VARCHAR(26) NOT NULL, \n\tselection_id VARCHAR(26) NOT NULL, \n\trecovery_id VARCHAR(26) NOT NULL, \n\tchunk_index BIGINT NOT NULL, \n\trequest_hash VARCHAR(64) NOT NULL, \n\trequest_snapshot TEXT NOT NULL, \n\treceipt_snapshot TEXT NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\taudit_event_id VARCHAR(26) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_recovery_chunk_index CHECK (COALESCE((typeof(chunk_index) = 'integer' AND chunk_index >= 0), 0)), \n\tCONSTRAINT ck_recovery_request_snapshot CHECK (COALESCE((CASE WHEN json_valid(request_snapshot) THEN json_type(request_snapshot) = 'object' ELSE 0 END), 0)), \n\tCONSTRAINT ck_recovery_receipt_snapshot CHECK (COALESCE((CASE WHEN json_valid(receipt_snapshot) THEN json_type(receipt_snapshot) = 'object' ELSE 0 END), 0)), \n\tUNIQUE (recovery_id, chunk_index), \n\tFOREIGN KEY(selection_id, recovery_id) REFERENCES payment_selection_recoveries (selection_id, id), \n\tFOREIGN KEY(audit_event_id) REFERENCES audit_events (id)\n)", "CREATE TABLE payment_selection_recovery_items (\n\tid VARCHAR(26) NOT NULL, \n\tselection_id VARCHAR(26) NOT NULL, \n\trecovery_id VARCHAR(26) NOT NULL, \n\tentry_index BIGINT NOT NULL, \n\tinvoice_id VARCHAR(26) NOT NULL, \n\tinvoice_type VARCHAR(32) NOT NULL, \n\taction VARCHAR(16) NOT NULL, \n\tobserved_invoice_version BIGINT NOT NULL, \n\tamount_minor_units BIGINT, \n\tcurrency VARCHAR(3), \n\tamount_origin VARCHAR(24), \n\tretained_calculation_revision_id VARCHAR(26), \n\tattempted_calculated_minor_units BIGINT, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\taudit_event_id VARCHAR(26) NOT NULL, \n\tPRIMARY KEY (id), \n\tCONSTRAINT ck_recovery_entry_index CHECK (COALESCE((typeof(entry_index) = 'integer' AND entry_index >= 1), 0)), \n\tCONSTRAINT ck_recovery_observed_invoice_version CHECK (COALESCE((typeof(observed_invoice_version) = 'integer' AND observed_invoice_version >= 1), 0)), \n\tCONSTRAINT ck_recovery_amount_minor_units CHECK (COALESCE((amount_minor_units IS NULL OR (typeof(amount_minor_units) = 'integer' AND amount_minor_units >= 0)), 0)), \n\tCONSTRAINT ck_recovery_attempted_calculated_minor_units CHECK (COALESCE((attempted_calculated_minor_units IS NULL OR (typeof(attempted_calculated_minor_units) = 'integer' AND attempted_calculated_minor_units >= 0)), 0)), \n\tCONSTRAINT ck_recovery_invoice_type CHECK (COALESCE((invoice_type = 'invoice'), 0)), \n\tCONSTRAINT ck_recovery_entry_shape CHECK (COALESCE(((action = 'remove' AND amount_minor_units IS NULL AND currency IS NULL AND amount_origin IS NULL AND retained_calculation_revision_id IS NULL AND attempted_calculated_minor_units IS NULL) OR (action = 'calculate' AND amount_minor_units IS NULL AND currency IS NULL AND amount_origin IS NULL AND retained_calculation_revision_id IS NULL) OR (action = 'set' AND currency IS NOT NULL AND attempted_calculated_minor_units IS NULL AND ((amount_origin = 'entered' AND amount_minor_units IS NOT NULL AND retained_calculation_revision_id IS NULL) OR (amount_origin = 'unresolved' AND amount_minor_units IS NULL AND retained_calculation_revision_id IS NULL) OR (amount_origin = 'calculated' AND amount_minor_units IS NOT NULL AND retained_calculation_revision_id IS NOT NULL)))), 0)), \n\tUNIQUE (recovery_id, entry_index), \n\tUNIQUE (recovery_id, invoice_id), \n\tFOREIGN KEY(selection_id, recovery_id) REFERENCES payment_selection_recoveries (selection_id, id), \n\tFOREIGN KEY(selection_id, retained_calculation_revision_id) REFERENCES payment_selection_revisions (selection_id, id), \n\tFOREIGN KEY(invoice_id, invoice_type) REFERENCES transactions (id, type), \n\tFOREIGN KEY(audit_event_id) REFERENCES audit_events (id)\n)", 'CREATE INDEX ix_recovery_item_invoice ON payment_selection_recovery_items (invoice_id, recovery_id)', 'CREATE INDEX ix_recovery_item_selection ON payment_selection_recovery_items (selection_id, invoice_id)', 'CREATE TABLE payment_selection_recovery_active (\n\tselection_id VARCHAR(26) NOT NULL, \n\trecovery_id VARCHAR(26) NOT NULL, \n\tcreated_at VARCHAR(32) NOT NULL, \n\tcreated_by VARCHAR(26) NOT NULL, \n\tcreated_via VARCHAR(16) NOT NULL, \n\taudit_event_id VARCHAR(26) NOT NULL, \n\tPRIMARY KEY (selection_id), \n\tUNIQUE (recovery_id), \n\tFOREIGN KEY(selection_id) REFERENCES payment_selections (id), \n\tFOREIGN KEY(selection_id, recovery_id) REFERENCES payment_selection_recoveries (selection_id, id), \n\tFOREIGN KEY(audit_event_id) REFERENCES audit_events (id)\n)')

GUARDS = ("CREATE TRIGGER payment_selection_recovery_chunks_no_update BEFORE UPDATE ON payment_selection_recovery_chunks BEGIN SELECT RAISE(ABORT, 'immutable recovery evidence'); END", "CREATE TRIGGER payment_selection_recovery_chunks_no_delete BEFORE DELETE ON payment_selection_recovery_chunks BEGIN SELECT RAISE(ABORT, 'immutable recovery evidence'); END", "CREATE TRIGGER payment_selection_recovery_chunks_uploading BEFORE INSERT ON payment_selection_recovery_chunks WHEN NOT EXISTS (SELECT 1 FROM payment_selection_recoveries r JOIN payment_selection_recovery_active a ON a.recovery_id=r.id WHERE r.id=NEW.recovery_id AND r.state='uploading') BEGIN SELECT RAISE(ABORT, 'recovery is not uploading'); END", "CREATE TRIGGER payment_selection_recovery_items_no_update BEFORE UPDATE ON payment_selection_recovery_items BEGIN SELECT RAISE(ABORT, 'immutable recovery evidence'); END", "CREATE TRIGGER payment_selection_recovery_items_no_delete BEFORE DELETE ON payment_selection_recovery_items BEGIN SELECT RAISE(ABORT, 'immutable recovery evidence'); END", "CREATE TRIGGER payment_selection_recovery_items_uploading BEFORE INSERT ON payment_selection_recovery_items WHEN NOT EXISTS (SELECT 1 FROM payment_selection_recoveries r JOIN payment_selection_recovery_active a ON a.recovery_id=r.id WHERE r.id=NEW.recovery_id AND r.state='uploading') BEGIN SELECT RAISE(ABORT, 'recovery is not uploading'); END", "CREATE TRIGGER recovery_no_delete BEFORE DELETE ON payment_selection_recoveries BEGIN SELECT RAISE(ABORT, 'immutable recovery identity'); END", "CREATE TRIGGER recovery_transition BEFORE UPDATE ON payment_selection_recoveries WHEN NEW.id IS NOT OLD.id OR NEW.selection_id IS NOT OLD.selection_id OR NEW.recovery_key IS NOT OLD.recovery_key OR NEW.request_schema_version IS NOT OLD.request_schema_version OR NEW.attempt_generation IS NOT OLD.attempt_generation OR NEW.local_baseline_revision_id IS NOT OLD.local_baseline_revision_id OR NEW.anchor_revision_id IS NOT OLD.anchor_revision_id OR NEW.anchor_selection_version IS NOT OLD.anchor_selection_version OR NEW.header_intent IS NOT OLD.header_intent OR NEW.declared_entry_count IS NOT OLD.declared_entry_count OR NEW.intent_hash IS NOT OLD.intent_hash OR NEW.created_at IS NOT OLD.created_at OR NEW.created_by IS NOT OLD.created_by OR NEW.created_via IS NOT OLD.created_via OR NEW.audit_event_id IS NOT OLD.audit_event_id OR NEW.begin_request_snapshot IS NOT OLD.begin_request_snapshot OR NEW.begin_request_hash IS NOT OLD.begin_request_hash OR NEW.begin_receipt_snapshot IS NOT OLD.begin_receipt_snapshot OR OLD.state IN ('applied','aborted','superseded') OR NEW.version <> OLD.version+1 OR (OLD.state='sealed' AND NEW.state NOT IN ('applied','aborted','superseded')) OR (OLD.state='uploading' AND NEW.state NOT IN ('uploading','sealed','aborted','superseded')) OR (OLD.seal_receipt_snapshot IS NOT NULL AND (NEW.seal_receipt_snapshot IS NOT OLD.seal_receipt_snapshot OR NEW.seal_request_hash IS NOT OLD.seal_request_hash OR NEW.seal_request_snapshot IS NOT OLD.seal_request_snapshot)) BEGIN SELECT RAISE(ABORT, 'invalid recovery transition'); END", "CREATE TRIGGER recovery_active_no_update BEFORE UPDATE ON payment_selection_recovery_active BEGIN SELECT RAISE(ABORT, 'replace barrier atomically'); END", "CREATE TRIGGER recovery_initial BEFORE INSERT ON payment_selection_recoveries WHEN NEW.state <> 'uploading' OR NEW.version <> 1 OR NOT EXISTS (SELECT 1 FROM payment_selections s WHERE s.id=NEW.selection_id AND s.state='open' AND s.version=NEW.anchor_selection_version AND s.current_revision_id=NEW.anchor_revision_id) BEGIN SELECT RAISE(ABORT, 'invalid recovery anchor'); END", "CREATE TRIGGER recovery_active_insert BEFORE INSERT ON payment_selection_recovery_active WHEN NOT EXISTS (SELECT 1 FROM payment_selection_recoveries r JOIN payment_selections s ON s.id=r.selection_id WHERE r.id=NEW.recovery_id AND r.selection_id=NEW.selection_id AND r.state='uploading' AND s.state='open') BEGIN SELECT RAISE(ABORT, 'invalid recovery barrier'); END", "CREATE TRIGGER recovery_active_delete BEFORE DELETE ON payment_selection_recovery_active WHEN EXISTS (SELECT 1 FROM payment_selection_recoveries r WHERE r.id=OLD.recovery_id AND r.state IN ('uploading','sealed')) BEGIN SELECT RAISE(ABORT, 'active recovery must be resolved atomically'); END", "CREATE TRIGGER recovery_item_range BEFORE INSERT ON payment_selection_recovery_items WHEN NEW.entry_index>(SELECT declared_entry_count FROM payment_selection_recoveries WHERE id=NEW.recovery_id) OR EXISTS (SELECT 1 FROM payment_selection_recovery_items i WHERE i.recovery_id=NEW.recovery_id AND ((i.entry_index<NEW.entry_index AND i.invoice_id>=NEW.invoice_id COLLATE BINARY) OR (i.entry_index>NEW.entry_index AND i.invoice_id<=NEW.invoice_id COLLATE BINARY))) BEGIN SELECT RAISE(ABORT, 'invalid recovery item range or order'); END")

def upgrade():
    for statement in DDL + GUARDS:
        op.execute(statement)

def downgrade():
    raise RuntimeError("Recovery history cannot be deleted")

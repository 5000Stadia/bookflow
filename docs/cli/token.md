<!-- Generated by `bookflow docs generate`; do not edit this file directly. -->

# `token` commands

## `token issue`

Issue a bearer token a program can send to the host; the secret is shown once.

| Contract | Value |
|---|---|
| Scope | hub |
| Kind | write |
| Required role | human self-service; a human hub administrator may issue for another user |
| Capability | token |
| Feature | — |
| HTTP | `POST /commands/token.issue` |

### CLI

`bookflow token issue --label "agent client" --days 30 --json`

### Input

| JSON field | CLI input | Type | Required | Nullable | Default | Description and constraints |
|---|---|---|---|---|---|---|
| `user` | `--user` | string \| null | no | yes | null | Username or id of the user the token acts as; defaults to you |
| `label` | `--label` | string | yes | no | — | What the token is for; it names the client in the audit trail; minimum length 1; maximum length 128 |
| `days` | `--days` | integer \| null | no | yes | null | Days until it expires; omitted means it never expires |
| `principal` | `--principal` | string \| null | no | yes | null | The human an agent token acts on behalf of; only for agent users |

### Command and context options

| Option | Meaning |
|---|---|
| `--json` | Print one JSON object. |
| `--data-root TEXT` | Data root; otherwise `BOOKFLOW_DATA_ROOT`, then `~/.bookflow`. |
| `--dry-run` | Validate and preview without writing. |
| `--reason TEXT` | Short reason for the write. |
| `--source-ref TEXT` | Identifier of the source that triggered the write. |
| `--interactive` | Prompt for input fields not supplied as arguments or options. |

### HTTP

Route: `POST /commands/token.issue`

Send the input object as JSON. Authentication may instead come from a browser session cookie.

| Header | Requirement | Meaning |
|---|---|---|
| `Authorization` | required for bearer clients | `Bearer <secret>` |
| `X-Bookflow-Client-Name` | optional | Stable caller name recorded in audit |
| `X-Bookflow-Client-Version` | optional | Caller version recorded in audit |
| `X-Bookflow-Reason` | conditional | Short reason; an agent or system write needs this or an active directive |
| `X-Bookflow-Source-Ref` | optional | Identifier of the source that triggered the write |

### Output

| JSON field | Type | Required | Nullable | Default | Description |
|---|---|---|---|---|---|
| `dry_run` | boolean | no | no | false | — |
| `warnings` | array[string] | no | no | [] | — |
| `token_id` | string | yes | no | — | — |
| `user_id` | string | yes | no | — | — |
| `username` | string | yes | no | — | — |
| `label` | string \| null | yes | yes | — | — |
| `expires_at` | string \| null | yes | yes | — | — |
| `secret` | string | yes | no | — | — |
| `message` | string | yes | no | — | — |

Example JSON output:

```json
{
  "dry_run": false,
  "expires_at": null,
  "label": null,
  "message": "value",
  "secret": "value",
  "token_id": "01ARZ3NDEKTSV4RRFFQ69G5FAV",
  "user_id": "01ARZ3NDEKTSV4RRFFQ69G5FAV",
  "username": "value",
  "warnings": []
}
```

### Errors

| Code | Meaning |
|---|---|
| `E_COMPANY_AMBIGUOUS` | More than one company matches; use the id or Organization/Company. |
| `E_COMPANY_NOT_FOUND` | No such company. |
| `E_CONFIG_INVALID` | The configuration file could not be read. |
| `E_CONTEXT_IN_INPUT` | Input contains a context field. |
| `E_DB_BUSY` | Another Bookflow command is running on this data root. |
| `E_FEATURE_DISABLED` | This feature is not enabled for the company. |
| `E_FS_UNKNOWN` | The filesystem type of the path could not be determined. |
| `E_INTERNAL` | Internal failure. |
| `E_IO` | A filesystem operation failed. |
| `E_MIGRATION_FAILED` | A schema migration failed; the database was backed up first and is unchanged. |
| `E_NETWORK_SHARE` | The path is on a network filesystem, which Bookflow refuses to use. |
| `E_NOT_INITIALIZED` | The data root is not initialized; run `bookflow init`. |
| `E_NO_ACTOR` | This login is not mapped to a Bookflow user. |
| `E_ORGANIZATION_NOT_FOUND` | No such organization. |
| `E_PERMISSION` | The acting user may not run this command here. |
| `E_REASON_REQUIRED` | Writes by an agent need --reason or --directive. |
| `E_SCHEMA_BEHIND` | The database schema is behind this version of Bookflow; run `bookflow upgrade`. |
| `E_SCHEMA_UNKNOWN` | The database schema revision is not known to this version of Bookflow; upgrade Bookflow. |
| `E_UNAUTHENTICATED` | No valid credential: log in, or send a bearer token. |
| `E_USAGE` | Invalid command syntax. |
| `E_USER_NOT_FOUND` | No such user. |
| `E_VALIDATION` | Invalid input. |

## `token list`

List the bearer tokens you may see; hub admins see everyone's.

| Contract | Value |
|---|---|
| Scope | hub |
| Kind | read |
| Required role | own tokens; a hub administrator may list another user's tokens |
| Capability | token |
| Feature | — |
| HTTP | `POST /commands/token.list` |

### CLI

`bookflow token list --json`

### Input

| JSON field | CLI input | Type | Required | Nullable | Default | Description and constraints |
|---|---|---|---|---|---|---|
| `user` | `--user` | string \| null | no | yes | null | Username or id whose tokens to list; hub admins only, for anyone but themselves |
| `include_revoked` | `--include-revoked` | boolean | no | no | false | Also list revoked and expired tokens |

### Command and context options

| Option | Meaning |
|---|---|
| `--json` | Print one JSON object. |
| `--data-root TEXT` | Data root; otherwise `BOOKFLOW_DATA_ROOT`, then `~/.bookflow`. |

### HTTP

Route: `POST /commands/token.list`

Send the input object as JSON. Authentication may instead come from a browser session cookie.

| Header | Requirement | Meaning |
|---|---|---|
| `Authorization` | required for bearer clients | `Bearer <secret>` |
| `X-Bookflow-Client-Name` | optional | Stable caller name recorded in audit |
| `X-Bookflow-Client-Version` | optional | Caller version recorded in audit |

### Output

| JSON field | Type | Required | Nullable | Default | Description |
|---|---|---|---|---|---|
| `items` | array[object] | yes | no | — | — |
| `items[].id` | string | yes | no | — | — |
| `items[].version` | integer | yes | no | — | — |
| `items[].created_at` | string | yes | no | — | — |
| `items[].created_by` | string | yes | no | — | — |
| `items[].created_via` | string | yes | no | — | — |
| `items[].updated_at` | string | yes | no | — | — |
| `items[].updated_by` | string | yes | no | — | — |
| `items[].updated_via` | string | yes | no | — | — |
| `items[].token_id` | string | yes | no | — | — |
| `items[].user_id` | string | yes | no | — | — |
| `items[].username` | string \| null | yes | yes | — | — |
| `items[].on_behalf_of` | string \| null | yes | yes | — | — |
| `items[].kind` | string | yes | no | — | — |
| `items[].label` | string \| null | yes | yes | — | — |
| `items[].expires_at` | string \| null | yes | yes | — | — |
| `items[].last_used_at` | string \| null | yes | yes | — | — |
| `items[].revoked_at` | string \| null | yes | yes | — | — |
| `count` | integer | yes | no | — | — |

Example JSON output:

```json
{
  "count": 1,
  "items": []
}
```

### Errors

| Code | Meaning |
|---|---|
| `E_COMPANY_AMBIGUOUS` | More than one company matches; use the id or Organization/Company. |
| `E_COMPANY_NOT_FOUND` | No such company. |
| `E_CONFIG_INVALID` | The configuration file could not be read. |
| `E_CONTEXT_IN_INPUT` | Input contains a context field. |
| `E_DB_BUSY` | Another Bookflow command is running on this data root. |
| `E_FEATURE_DISABLED` | This feature is not enabled for the company. |
| `E_FS_UNKNOWN` | The filesystem type of the path could not be determined. |
| `E_INTERNAL` | Internal failure. |
| `E_IO` | A filesystem operation failed. |
| `E_MIGRATION_FAILED` | A schema migration failed; the database was backed up first and is unchanged. |
| `E_NETWORK_SHARE` | The path is on a network filesystem, which Bookflow refuses to use. |
| `E_NOT_INITIALIZED` | The data root is not initialized; run `bookflow init`. |
| `E_NO_ACTOR` | This login is not mapped to a Bookflow user. |
| `E_ORGANIZATION_NOT_FOUND` | No such organization. |
| `E_PERMISSION` | The acting user may not run this command here. |
| `E_REASON_REQUIRED` | Writes by an agent need --reason or --directive. |
| `E_SCHEMA_BEHIND` | The database schema is behind this version of Bookflow; run `bookflow upgrade`. |
| `E_SCHEMA_UNKNOWN` | The database schema revision is not known to this version of Bookflow; upgrade Bookflow. |
| `E_UNAUTHENTICATED` | No valid credential: log in, or send a bearer token. |
| `E_USAGE` | Invalid command syntax. |
| `E_USER_NOT_FOUND` | No such user. |
| `E_VALIDATION` | Invalid input. |

## `token revoke`

Revoke a bearer token so it stops working immediately.

| Contract | Value |
|---|---|
| Scope | hub |
| Kind | write |
| Required role | own tokens; a hub administrator may revoke another user's token |
| Capability | token |
| Feature | — |
| HTTP | `POST /commands/token.revoke` |

### CLI

`bookflow token revoke 01ARZ3NDEKTSV4RRFFQ69G5FAV --json`

### Input

| JSON field | CLI input | Type | Required | Nullable | Default | Description and constraints |
|---|---|---|---|---|---|---|
| `token` | `TOKEN` | string | yes | no | — | Token id, as `token list` reports it |

### Command and context options

| Option | Meaning |
|---|---|
| `--json` | Print one JSON object. |
| `--data-root TEXT` | Data root; otherwise `BOOKFLOW_DATA_ROOT`, then `~/.bookflow`. |
| `--dry-run` | Validate and preview without writing. |
| `--reason TEXT` | Short reason for the write. |
| `--source-ref TEXT` | Identifier of the source that triggered the write. |
| `--interactive` | Prompt for input fields not supplied as arguments or options. |

### HTTP

Route: `POST /commands/token.revoke`

Send the input object as JSON. Authentication may instead come from a browser session cookie.

| Header | Requirement | Meaning |
|---|---|---|
| `Authorization` | required for bearer clients | `Bearer <secret>` |
| `X-Bookflow-Client-Name` | optional | Stable caller name recorded in audit |
| `X-Bookflow-Client-Version` | optional | Caller version recorded in audit |
| `X-Bookflow-Reason` | conditional | Short reason; an agent or system write needs this or an active directive |
| `X-Bookflow-Source-Ref` | optional | Identifier of the source that triggered the write |

### Output

| JSON field | Type | Required | Nullable | Default | Description |
|---|---|---|---|---|---|
| `dry_run` | boolean | no | no | false | — |
| `warnings` | array[string] | no | no | [] | — |
| `token_id` | string | yes | no | — | — |
| `user_id` | string | yes | no | — | — |
| `username` | string \| null | yes | yes | — | — |
| `label` | string \| null | yes | yes | — | — |
| `revoked_at` | string \| null | yes | yes | — | — |
| `changed` | boolean | yes | no | — | — |

Example JSON output:

```json
{
  "changed": false,
  "dry_run": false,
  "label": null,
  "revoked_at": null,
  "token_id": "01ARZ3NDEKTSV4RRFFQ69G5FAV",
  "user_id": "01ARZ3NDEKTSV4RRFFQ69G5FAV",
  "username": null,
  "warnings": []
}
```

### Errors

| Code | Meaning |
|---|---|
| `E_COMPANY_AMBIGUOUS` | More than one company matches; use the id or Organization/Company. |
| `E_COMPANY_NOT_FOUND` | No such company. |
| `E_CONFIG_INVALID` | The configuration file could not be read. |
| `E_CONTEXT_IN_INPUT` | Input contains a context field. |
| `E_DB_BUSY` | Another Bookflow command is running on this data root. |
| `E_FEATURE_DISABLED` | This feature is not enabled for the company. |
| `E_FS_UNKNOWN` | The filesystem type of the path could not be determined. |
| `E_INTERNAL` | Internal failure. |
| `E_IO` | A filesystem operation failed. |
| `E_MIGRATION_FAILED` | A schema migration failed; the database was backed up first and is unchanged. |
| `E_NETWORK_SHARE` | The path is on a network filesystem, which Bookflow refuses to use. |
| `E_NOT_INITIALIZED` | The data root is not initialized; run `bookflow init`. |
| `E_NO_ACTOR` | This login is not mapped to a Bookflow user. |
| `E_ORGANIZATION_NOT_FOUND` | No such organization. |
| `E_PERMISSION` | The acting user may not run this command here. |
| `E_REASON_REQUIRED` | Writes by an agent need --reason or --directive. |
| `E_SCHEMA_BEHIND` | The database schema is behind this version of Bookflow; run `bookflow upgrade`. |
| `E_SCHEMA_UNKNOWN` | The database schema revision is not known to this version of Bookflow; upgrade Bookflow. |
| `E_TOKEN_NOT_FOUND` | No such token. |
| `E_UNAUTHENTICATED` | No valid credential: log in, or send a bearer token. |
| `E_USAGE` | Invalid command syntax. |
| `E_VALIDATION` | Invalid input. |

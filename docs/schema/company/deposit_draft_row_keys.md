<!-- Generated by `bookflow docs generate`; do not edit this file directly. -->

# `company.deposit_draft_row_keys`

Private immutable deposit composition draft_row_keys.

Database: `company`.

| Column | SQL type | Nullable | Default | Key | Indexes | References | Meaning |
|---|---|---|---|---|---|---|---|
| `id` | VARCHAR(26) | no | — | primary key 1, unique with draft_id + id, unique with draft_id + id + ordinal + kind | — | — | Deposit durable composition id. |
| `draft_id` | VARCHAR(26) | no | — | unique with draft_id + id, unique with draft_id + id + ordinal + kind, unique with draft_id + ordinal | — | deposit_drafts.id | Deposit durable composition draft id. |
| `kind` | TEXT | no | — | unique with draft_id + id + ordinal + kind | — | — | Deposit durable composition kind. |
| `ordinal` | BIGINT | no | — | unique with draft_id + id + ordinal + kind, unique with draft_id + ordinal | — | — | Deposit durable composition ordinal. |
| `edit_transaction_id` | VARCHAR(26) | yes | — | — | — | deposit_row_keys.transaction_id | Deposit durable composition edit transaction id. |
| `original_row_id` | VARCHAR(26) | yes | — | — | ix_deposit_draft_row_keys_original_row_id | deposit_row_keys.id | Deposit durable composition original row id. |
| `created_at` | TEXT | no | — | — | — | — | Deposit durable composition created at. |
| `created_by` | VARCHAR(26) | no | — | — | — | — | Deposit durable composition created by. |
| `created_via` | TEXT | no | — | — | — | — | Deposit durable composition created via. |
| `audit_event_id` | VARCHAR(26) | no | — | — | — | audit_events.id | Deposit durable composition audit event id. |

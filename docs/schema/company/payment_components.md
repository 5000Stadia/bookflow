<!-- Generated by `bookflow docs generate`; do not edit this file directly. -->

# `company.payment_components`

Positive immutable capacities for a receipt revision; zero capacity has no row.

Database: `company`.

| Column | SQL type | Nullable | Default | Key | Indexes | References | Meaning |
|---|---|---|---|---|---|---|---|
| `id` | VARCHAR(26) | no | — | primary key 1, unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | — | Revision-local receipt capacity. |
| `transaction_id` | VARCHAR(26) | no | — | unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | document_lines.transaction_id, payment_component_keys.transaction_id, payment_profiles.transaction_id | Owning payment. |
| `revision_id` | VARCHAR(26) | no | — | unique with revision_id + component_key_id, unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | document_lines.revision_id, payment_profiles.revision_id | Owning payment revision. |
| `document_line_id` | VARCHAR(26) | no | — | unique with transaction_id + revision_id + document_line_id + id | — | document_lines.id | Owned payment envelope. |
| `component_key_id` | VARCHAR(26) | no | — | unique with revision_id + component_key_id | — | payment_component_keys.id | Permanent component identity. |
| `amount_minor_units` | BIGINT | no | — | — | — | — | Positive capacity of this revision component. |
| `currency` | VARCHAR(3) | no | — | — | — | — | Home currency. |
| `component_snapshot` | TEXT | no | — | — | — | — | Captured exact-party and account facts. |
| `created_at` | VARCHAR(32) | no | — | — | — | — | UTC recorded timestamp. |
| `created_by` | VARCHAR(26) | no | — | — | — | — | Principal creating this record. |
| `created_via` | VARCHAR(16) | no | — | — | — | — | Interface creating this record. |
| `audit_event_id` | VARCHAR(26) | no | — | — | — | audit_events.id | Owned creation audit event. |

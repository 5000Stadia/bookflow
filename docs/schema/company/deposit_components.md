<!-- Generated by `bookflow docs generate`; do not edit this file directly. -->

# `company.deposit_components`

Owned funding attribution; foreign receipt line IDs live only in captured provenance.

Database: `company`.

| Column | SQL type | Nullable | Default | Key | Indexes | References | Meaning |
|---|---|---|---|---|---|---|---|
| `id` | VARCHAR(26) | no | — | primary key 1, unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | — | Revision-local funding component. |
| `transaction_id` | VARCHAR(26) | no | — | unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | deposit_component_keys.transaction_id, deposit_profiles.transaction_id, deposit_row_keys.transaction_id, document_lines.transaction_id | Owning deposit. |
| `revision_id` | VARCHAR(26) | no | — | unique with revision_id + row_id + component_ordinal, unique with transaction_id + revision_id + document_line_id + id, unique with transaction_id + revision_id + id | — | deposit_profiles.revision_id, document_lines.revision_id | Owning revision. |
| `document_line_id` | VARCHAR(26) | no | — | unique with transaction_id + revision_id + document_line_id + id | — | document_lines.id | Deposit-owned envelope. |
| `row_id` | VARCHAR(26) | no | — | unique with revision_id + row_id + component_ordinal | — | deposit_component_keys.row_id, deposit_row_keys.id | Durable deposit row occurrence. |
| `component_ordinal` | BIGINT | no | — | unique with revision_id + row_id + component_ordinal | — | deposit_component_keys.ordinal | Owned component occurrence: zero for additional rows; positive for source and header. |
| `role` | TEXT | no | — | — | — | — | funding, offset or cash_back owned component. |
| `capacity` | BIGINT | no | — | — | — | — | Positive component amount; only funding supplies allocation capacity. |
| `currency` | TEXT | no | — | — | — | — | Home currency. |
| `facts_snapshot` | TEXT | no | — | — | — | — | Typed cash provenance, semantic key, captured source and occurrence facts. |
| `source_transaction_id` | VARCHAR(26) | yes | — | — | — | document_lines.transaction_id, posting_line_sources.transaction_id, posting_lines.transaction_id | Source receipt, null for additional funding. |
| `source_revision_id` | VARCHAR(26) | yes | — | — | — | document_lines.revision_id | Exact captured source revision. |
| `source_document_line_id` | VARCHAR(26) | yes | — | — | — | document_lines.id | Receipt-owned envelope, never the deposit envelope. |
| `source_posting_line_id` | VARCHAR(26) | yes | — | — | — | posting_lines.id | Captured UF debit line. |
| `source_attribution_id` | VARCHAR(26) | yes | — | — | — | posting_line_sources.id | Captured UF source attribution. |
| `created_at` | TEXT | no | — | — | — | — | UTC recorded time. |
| `created_by` | VARCHAR(26) | no | — | — | — | — | Execution principal. |
| `created_via` | TEXT | no | — | — | — | — | Execution interface. |
| `audit_event_id` | VARCHAR(26) | no | — | — | — | audit_events.id | Owning audit event. |

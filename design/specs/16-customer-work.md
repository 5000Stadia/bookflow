# 16 — Customer work documents

## Boundary and owning contracts

Implement the non-posting portion of [Customer work and billing](../customer-work-and-billing.md)
and [customer-work controls](../inventories/customer-work.md). Financial conversion,
partial/progress allocations and customer payments are the following increment;
no invoice/sales-receipt command is generated by operational completion here.
Ordinary sales keep their existing serialized facts, arithmetic and posting guards.

Home-currency service, non-inventory part and fixed other-charge lines use the
existing selling catalog. Inventory, assemblies, percentage charges, discounts,
groups, returns, time/expense allocation and foreign quoting remain with their
inventory/sales/source plans. A proposal or work order may contain no priced lines;
an estimate contains1–200 lines. Every priced line has a positive exact quantity;
zero prices and a zero quote total are allowed. Estimated cost is not a ledger expense.

The contractor work-order form, explicit acceptance evidence, ancestry links and
retained cancellation history are approved Bookflow extensions. The available
reference describes estimates and sales orders, but no comparable contractor
work-order form. Internal cost/markup stays internal by default; later output
templates must explicitly authorize disclosure. Job-master dates remain the source
of job-date reports; work-order dates describe that work order only.

## Commands and authority

The shared registry exposes these21 commands:

| Noun | Commands |
|---|---|
| proposal | create, update, copy, show, query, history, estimate |
| estimate | create, update, copy, show, query, history, work-order |
| work-order | create, update, copy, show, query, history, complete |

All are company scoped. Reads require member and `customer-work`; writes require
standard and `customer-work`, use company audit, and accept generic idempotency.
They are not ledger commands. Updates/copies/completion require a positive
expected_version. Create rejects supplied persistent line identities. Update
replaces the submitted ordered line collection as a whole; omitted lines retain
it. Existing line_id values must belong to that document, occur once, and preserve
identity. Only identities in the current revision can be submitted; removed line
identities are permanently retired and cannot be reintroduced. New lines receive new identities. Whole-document version conflicts never
merge disjoint fields. Every supplied null has an explicit clear/reject meaning.

Show selects current or a positive revision_number. Query filters exact customer,
number/title text, date interval, status, active (default true; null means both),
and inclusive minimum/maximum net amount. It orders by date then stable id,
limits50/default,200/max, and uses the existing scope/permission-hash-checked audit-watermark continuation.
History is revision-number ordered and bounded. Query summaries do not materialize
all lines. Referenced records resolve only in the selected company.

Every write supports dry-run with a resolved-facts fingerprint; execution checks
optional expected_facts_fingerprint. Browser Save requires a successful current
preview. Master changes after preview cause E_PREVIEW_STALE when they would change
newly resolved facts. Apply replans and independently validates the entire pending
work aggregate against the typed input immediately before persistence. A no-op
creates no version, revision or audit event. A context reason is required for
cancellation, supersession, reopening complete work, or revoking a recorded acceptance.

Errors include existing validation, not-found, inactive-reference, amount/range,
version, duplicate-number and stale-query/preview errors. Add E_WORK_DEPENDENCY for
incompatible accepted alternatives, frozen converted scope or an existing work-order
destination, and E_CONVERSION_KEY_REUSED for a durable key with different intent.
Both are HTTP409 and return the relevant source/destination identities where visible.

## Shared and kind-specific inputs

Create requires date, customer and title (1–200 characters). Number is optional;
automatic numbering is per kind, uses the existing sequences table under disjoint
proposal/estimate/work_order keys, and skips occupied numbers. Explicit numbers
must be unique within kind and do not advance the automatic sequence. Date is ISO
calendar date. Renumbering is versioned and preserves document identity.

All three kinds capture memo/internal note (2000), scope, inclusions, exclusions,
timing and commercial_terms (each10000), customer/job contact and billing/shipping
address, owned shipping-address selection, customer PO, terms, ship method/date,
sales rep, class, customer tax code/tax selection, price level, customer message,
typed custom fields and origins. Addresses and ordinary commercial fields retain
their current sales bounds. Shared defaults follow the existing customer/job
inheritance, explicit clear, use_defaults and refresh_defaults rules. Work documents
capture terms but do not invent invoice due dates, AR accounts, payment facts or
credit-limit warnings. Issuer/company business identity is snapshotted too.

Refactor the existing captured commercial profile into a common non-financial
base and the existing required-control-account sales profile, preserving the exact
old sales JSON shape. Header resolution explicitly distinguishes non-posting kinds.
Line resolution shares catalog/reference/quantity/unit/default selection. A quote
captures the current tax policy but is not rejected merely because that policy
recognizes liability on payment; the existing actual-sale restriction remains.
Referenced selling items and selected taxes must be valid current catalog choices;
ordinary unchanged facts retain their saved inactive references. A quote requires
no AR or deposit destination. Catalog income/tax-account references are captured
as catalog facts, without generating financial effects.

Proposal and estimate status: draft, open, accepted, declined, superseded, cancelled.
Create starts draft. Update explicitly records a decision_note (1–2000) on accepting,
declining or superseding; acceptance records its actual revision, UTC time and actor,
not an electronic-signature claim. Changing any agreed header/scope/price/line fact
of an accepted document returns it to draft and clears current acceptance; immutable
past acceptance remains in history. Memo and availability edits do not revoke it.
Direct setting from cancelled/declined/superseded to draft reopens with a context reason.
Allowed proposal/estimate transitions: draft may become open, accepted, declined or
cancelled; open may become draft, accepted, declined or cancelled; accepted may
become draft, superseded or cancelled with a reason; declined, superseded and
cancelled may only reopen to draft with a reason. Same-status updates are allowed.
Acceptance evidence names the revision that first accepted those agreed facts;
subsequent memo/availability/conversion-link revisions retain that reference.
An estimate also has expires_on (nullable ISO date not before issue date). Expiry is
a read-time warning, never an unrecorded status change. Accepting an expired estimate
requires an explicit acknowledge_expired boolean on that update.

Active/inactive availability is a separate bool, initially true, editable without
changing acceptance, completion or history. Inactive documents remain showable and
can be included in query. A conversion requires an active eligible source.

Work-order operational fields: priority low/normal/high/urgent (normal default),
site_address (defaults from captured shipping address), assignees (0–50 distinct
employee selectors, saved as id/label/version), scheduled_start/end, actual_start/end
(nullable ISO8601 timestamps with timezone, stored in UTC). End cannot precede
start; an end requires its start. Newly selected assignees must be active employees;
unchanged saved assignees may later be inactive. Status: draft, scheduled,
in_progress, on_hold, complete, cancelled. Scheduled needs scheduled_start;
in_progress needs actual_start; complete needs actual_start and actual_end.
`work-order complete` accepts explicit actual_start/end overrides, defaults missing
actual_end to command UTC time, and rejects a missing actual_start rather than
inventing when work began. Repeated completion with unchanged facts is a no-op.
Reopening complete work returns to in_progress with a reason and clears actual_end.
Cancelled work can reopen to draft with a reason. No operational status means invoiced
or paid: those are derived from future linked accounting documents. Between draft,
scheduled, in_progress and on_hold, explicit transitions are allowed subject to
the destination timestamp constraints. Any of those may cancel with a reason or
complete once all completion constraints hold. Complete and cancelled may only
reopen as specified above. Every resulting complete aggregate, on every write including same-status edits,
requires both timestamps and completed quantities equal to ordered quantities.
Changing scope text, adding/removing lines, changing ordered quantity or reducing
completed quantity on complete work requires explicitly reopening first (or in the
same update with a reason). This also applies to initially unpriced work.
Direct update to complete requires those completed quantities to be supplied or
already saved; only the complete command fills the remainder.

Work-order lines capture completed_quantity (exact nonnegative decimal,6places,
default0, at most ordered quantity). Proposal/estimate lines reject this input.
Operational edits may record partial completion; this is a Bookflow extension, not
a sales-order fulfillment parity claim. The explicit complete command sets remaining
line quantities to ordered quantities in its preview and committed revision. Zero
priced lines still require actual_start/end to complete the scope. Reopening retains
completed quantities until explicitly revised. Copy/conversion resets them to0.
Completion, billable eligibility and later billed consumption are independent.

## Line price and estimated cost

Capture item, description, quantity (positive decimal string,6places), unit and
factor, base quantity, class/tax code, catalog price rule, billable bool (true by
default), and existing catalog facts/origins. Each line also captures estimated
unit cost in home Money or unknown, its origin, pricing_basis and optional markup
percentage or explicit net amount. Costs are per selected unit; catalog base cost
is converted with the captured factor and half-even rounding to minor units.
Unknown cost is null, not zero. Manual cost must be nonnegative home Money.

One selling-price mode is authoritative:

- Catalog/default: use the existing selected price-level or standard-price calculation.
- Manual unit price: explicit unit_price; net is half-even(quantity_micro*price/1e6).
- Cost markup: explicit markup_percent in [-100,1000000] with at most6decimal
  places; require a known cost. Rate is half-even(cost_minor*(100e6+percent_micro)/100e6),
  then extend the quantity as above. Zero cost produces zero price, without division.
- Amount: explicit net_amount, nonnegative home Money. Quantity remains descriptive,
  selling unit price is null and the UI shows an amount-based line. It never invents
  a rounded rate that fails to extend to the entered amount.

Supplying more than one of unit_price, markup_percent and net_amount is invalid.
Explicit price_level together with markup/net_amount is invalid. An inherited
price-level may remain in the header but is not applied on those overridden lines.
A newly supplied non-null unit_price, markup_percent or net_amount selects that
mode and clears the other saved authoritative inputs and their mode origins. This
is identical for every pair of modes; the request cannot submit two such inputs,
even if one would equal its old value. Null is rejected for each authoritative
input; use_defaults unit_price explicitly returns to catalog. Explicit price_level
selects catalog pricing and clears a saved manual/markup/amount override unless a
unit_price is also supplied, in which case the manual rate wins and the rule is
captured as unused. Cost and its origin survive all price-mode switches unless
separately edited or reset; price origin becomes explicit for each manual switch.
Markup is distinct from a price-level adjustment percentage and from margin.

Ordinary edits retain the saved pricing mode. Quantity changes recalculate rate-
based extensions but retain amount-based net. A manual cost edit recalculates a
markup price, not manual/catalog/amount prices. Item/unit changes resolve their
new defaults, retaining explicitly overridden selected-unit cost/rate with a warning.
use_defaults unit_price returns to catalog mode and clears markup/net override;
use_defaults estimated_unit_cost returns to catalog cost. Explicit refresh captures
current defaults/rules while respecting retained explicit overrides.

Extend estimated cost using the same quantity arithmetic; display known_cost_total
and cost_complete separately. Estimated profit is net minus total estimated cost
only when every line cost is known; otherwise null. Prices, extensions, each tax
component and totals stay exact bounded int64; intermediates are unbounded integers.

Tax is computed per line/component from its authoritative net and the captured
rate with existing half-even arithmetic. Exempt/disabled tax remains explicit.
Zero taxes can be captured; there are no zero posting legs because there are no
posting legs at all. Examples: qty2, cost4.00, markup25 => rate5.00/net10.00;
qty2, explicit net10.01 => blank rate/net10.01; at8%tax =>0.80, gross10.81.
A quote grand total never represents cash received or an actual receivable.

## Alternatives, copies and operational conversion

Every estimate belongs to an estimate_group_id. A standalone estimate uses its
own id. Estimates converted from one proposal share that proposal's group identity.
At most one estimate in a group can be currently accepted, enforced in storage.
Choosing a different alternative requires explicitly superseding the previously
accepted one first. No silent group-wide acceptance/status mutations.

Copy requires canonical source id, expected_version, destination date and optional
number/title plus destination custom_fields/custom_field_kinds; estimate additionally
accepts expiry and copy_mode. Other source
fields cannot be overridden in copy: revise the resulting draft explicitly.
Copy may inspect any source status, including inactive; it creates an active draft.

`estimate copy` defaults to alternative: a new numbered draft in the same group.
An explicit copy_mode=independent creates a new scope/group; preview labels that
choice. Proposal/work-order copy is explicitly new independent scope. Copies keep
captured customer/scope/prices but reset acceptance, dates supplied for the new
record, operational status/schedule/actuals/assignees, billing consumption and
successful delivery. They retain a visible copy-of relation. New line identities
and billing roots are allocated. Source files/notes remain linked through ancestry.

`proposal estimate` takes canonical proposal id, expected_version, required
conversion_key (1–128 characters), destination date, optional number, optional
expiry, destination custom_fields/custom_field_kinds, and preview fingerprint. Source must be draft/open/accepted, active and
have at least one priced line. It creates an estimate draft carrying the exact
selected source revision, customer/scope/lines and eligible custom values.
Different explicit keys can produce alternative estimates. Proposal revisions
remain editable; no revision changes existing estimates.

`estimate work-order` has the same durable-key/version/date/number inputs and
optional scheduling/assignee fields and destination custom_fields/custom_field_kinds. It requires the group's accepted estimate
revision. It creates one draft work order carrying source facts and the estimate's
billing-root identities. It does not mark work scheduled, complete or invoiced.
There is at most one work-order destination per estimate identity, permanently;
a different key returns E_WORK_DEPENDENCY with the existing destination. Reopen
that work order when appropriate instead of duplicating the agreed work.

Conversion-key uniqueness is company-wide and permanent, separate from generic
request-cache expiry. Store hash(key), canonical request hash, source id/revision/
version, destination id/initial revision and relation. Same key+intent returns the
original destination with its current state even after either document changes or
the generic cache expires. Different intent with that key rejects. Authorization
is checked before replay. Key hashing compares canonical typed request with the
explicit source id/version and caller fields; exclude preview fingerprint, context
clock and generated ids. Dry-run never reserves a key, number or identity.

Destination creation, durable key, all lineage, source version/provenance changes
and audit are one company transaction. A first conversion increments the source
version and appends a same-facts linked revision, making concurrent use of a stale
source version visible. Replay appends nothing. Proposal may convert again with its
new version. Source and destination show both birth revision and current state.

An estimate with a work-order destination cannot revise agreed customer, scope,
addresses, terms, line item/unit/quantity/price/tax facts or revoke acceptance.
The converted work-order header also binds its customer, captured commercial
profile (addresses, terms, PO, tax/rule choices), title, scope, inclusions, exclusions,
timing and commercial_terms to the accepted source. It may change its own number,
date, memo, availability, site_address, operational fields and destination custom
values; those do not alter the source agreement. Agreed header changes reject
atomically with E_WORK_DEPENDENCY.
Its linked work-order lines retain those source economics and cannot be removed or
repriced. Operational scheduling, assignees, memo, availability, status and billable
flags remain editable. New explicit work-order lines are independent added scope
with new billing roots and are visibly distinguished from quoted source lines.
Formal change-order approval and edits of already billed scope belong to the
financial conversion increment; no silent rewrite is exposed here.

Proposal->estimate creates new estimate billing roots, because alternatives can
quote different economics. Estimate->work-order inherits roots. Ordinary independent
copies create new roots and identify themselves as independent scope in preview.
Each root retains a source-line revision link for inspection. No billing allocation
exists yet; these identities are the seam for the next owning plan, not a billed
quantity cache and not a single invoice pointer.

## Storage and integration

Add co0010 with revision-local DDL, preserving every old row. The sole old-table
definition change widens custom_field_scopes record_type CHECK to admit proposal
and work_order. Preserve all columns, indexes, triggers, local views and existing
scope rows through a transactional rebuild; rollback restores the original schema
and rows on failure. All other old table definitions remain unchanged.
Add work_documents (primary identity/kind/number/version/current revision/status/
active/estimate group/provenance), work_revisions (immutable whole snapshots plus
queryable date/customer/net/tax/gross and decision facts), work_line_identities
(document owner, root identity and optional source revision-line), work_lines
(immutable ordered facts, exact amounts/costs, profile and quoted component snapshot),
and work_links (source/initial destination revision, relation, durable key/request hash).

All revision/line/link owner references have enforced company-local foreign keys;
composite constraints fence line identity and revision ownership. Current revision
belongs to its header. Stable root/source links cannot be caller-assigned or moved.
Unique(kind,number), unique(document,revision_number), unique(revision,line_no),
unique(revision,line_identity), accepted-estimate-group uniqueness, destination
ancestry and estimate-work-order cardinality are storage constraints. Immutable
revision/line/link/identity rows have rejection-only update/delete triggers. Current
headers update with a version precondition. Typed snapshots are independently
validated against referenced ids, totals, lineage, input and allowed transitions.

Extend annotation targets work_document, work_revision and work_line; exact source
links let activity/detail expose original notes/files without moving or duplicating
bytes. Show source annotations as internal and never automatically render them as
customer-facing scope. Custom scopes proposal, estimate and work_order use the
existing typed owner slots and immutable revision snapshots. Conversion copies
only definitions enabled for the destination scope, preserving kind/value facts
with destination-owned value ids; omitted values are listed in preview. Copies and operational conversions preserve captured customer/item/unit/tax/account
facts even if those masters are now inactive, with explicit preview warnings; the
source document itself must be active for conversion. Referenced identities must
still exist in this company, and no current master facts replace the saved economics.
New explicit selections, including destination assignees, require current eligibility.
This is a non-posting historical carry rule; financial conversion will validate its
own current posting eligibility.

Destination custom values follow creation requirements: carry only currently active
definitions enabled for the destination and currently eligible choices with matching
kinds; list every omitted value/reason in preview. The destination patch is applied
over carried values, then ordinary current defaults supply any still-absent eligible
values. Required missing values reject with the exact definition ids and can be
provided through that patch in the same copy/conversion. Explicit null clears a
carried value but cannot evade a required field. New destination-owned value ids
are always allocated; carried kind/value/label snapshots remain inspectable together
with their source snapshot, and current destination validation does not rewrite the
source. The same rules cover copies after definitions change. Any explicit
new selection validates current definition/choice eligibility. Existing attachment
integrity and source ownership do not change.

Implement dedicated work_models/facts/schema/service/validation modules and thin
work_cmds registry adapters. Shared-commercial factoring is restricted to reusable
fact/default resolution; ordinary sale persistence/validation stays independent.
Browser navigation adds a customer-work section, searchable lists, readable detail/
history/source links and forms for all21commands. Price/cost modes, draft/acceptance,
operational state and explicit conversion preview are readable at1280and390pixels.
Forms preserve entered values on conflict/stale errors, use cryptorandom retry keys,
and never call a financial or delivery command as a side effect of Save/Complete.

## Witnesses and staged inventory

CW01–08,10,12–18,28,31,44–45 are covered to the exact boundary above. CW05 includes
cost/markup/catalog/amount pricing; CW08 includes alternative selection and separate
availability. CW19 is lineage foundation only. CW20–27,29–30 and32–41 require the
next financial/payment/cost/report plans; CW11 and42–43 require templates/rendering/
delivery; CW46–48 require inventory, special sales lines and source capture. No
staged control is reported as implemented by this row.

CW09 is partial: this row always exposes work documents and enforces unique
numbers; it never automatically closes estimates after operational conversion.
Estimate/progress feature visibility and close-after-financial-conversion preferences
are explicitly staged with Row17, including preservation when disabled. Duplicate
number warnings cannot relax the existing uniqueness contract. Zero-line print
suppression belongs to output templates. Default item markup percent/account belongs
to item-creation pricing, and default billable time/expenses and reimbursed-expense
income preferences belong to source-cost capture. Configurable job-status labels
belong to the job-master settings increment. No unsupported preference is added as
a nonfunctional switch. Reusable template lines will be a separate model allowing
blank quantities; saved operational quote lines keep positive quantities. Stock
sales orders, backorders, picked/packed/shipped and partial-vs-complete shipment
policy are a named future document/fulfillment increment under CW46.

Both existing demo scripts retain their old prefix and add proposal->alternative
estimates->accepted estimate->work order->completion, direct estimate/work-order,
independent copy, amount and markup lines, cost unknown/zero, a nonbillable line,
custom false/clear/history and notes/source-file links. Original GL/account/customer
balances and old attachment hashes remain exact; operational additions create
zero transactions, posting batches, posting lines or financial applications.

Meaningful tests cover all commands across library/CLI/HTTP, browser1280/390 journeys,
exact pricing/tax examples, captured-default/master edits, whole-version/no-op,
read-only/other-company isolation, malicious cross-document lines, immutable SQL
writes, conversion races and different-intent keys, cache expiry/copy/reopen, accepted
alternatives and source-edit guards, inherited annotations/custom ownership, fresh
and populated co9->co10 migration/rollback, existing-sale regression, demo extension
and generated docs/inventory/error matrix. Independent schema/lifecycle plan and
assembled-artifact review are required; routine UI/seed work uses proportional
self-checks. Live upgrade follows only after a byte-verified closed backup and
preservation rehearsal; never reset the human demo.

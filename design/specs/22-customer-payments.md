# Row22 — Customer payments, invoice settlement and cooperating interfaces

Status: complete proposal for independent plan review; not implementation authorization. Written 2026-09-05 against frozen `40c5243a6a156367c468d008cf70c0400a587c71` in `/tmp/bookflow-financial-version-history`. Parent owns intention, public design and integration. Row21 has independently passed and is closed in main; its diagnostic prerequisite is available. This proposal supersedes the exact-party-only *new receipt* alternative in `notes/payment-design-draft.md`.

## 1. Owning outcome and boundary

A human or authorized program can record new customer cash, distribute it among parent/job invoices, retain unapplied AR credit with an explicit owner, apply that existing compatible credit later, correct either document, inspect settlement history, unapply and void. All operations use the same registry, permissions, identities, exact accounting and immutable audit history through CLI, HTTP and browser. An agent creates work that a person can inspect/continue in the GUI and vice versa. No UI-only allocation or agent-only correction implementation qualifies as complete.

Immediate implementation interfaces are actual CLI, HTTP and workbench. Row22 CLOSURE also requires an executed same-registry MCP journey through Row9's actual reviewed adapter, covering discovery, authentication, context attribution, preview, execution, errors and durable recovery. Core work may proceed before Row9 finishes; Row22 cannot close until this evidence exists. Do not invent an endpoint or identity bootstrap while Row9 is absent. Registry readiness alone is not MCP evidence.

Implemented scope is home-currency receipts for EVERY currently supported nonstock invoice profile: service items, non-inventory parts, fixed other charges, ordinary unit-priced lines, amount-priced lines, and exact progress-allocated lines, including mixtures. Use authoritative stored net/tax component integers, never recreate amount-priced/progress amounts from quantity times price. One compatible AR account per payment; one payer can fund selected invoices in its current descendant family. Existing invoice/sales-receipt/work billing behavior remains. Payment is NOT a sales receipt and posts no revenue. Existing invoice-date tax liability postings remain; allocating receipt cents to tax does not enable the currently rejected payment-receipt tax-liability policy. Dependent credit/refund/discount/advance/banking/provider/deletion workflows remain required by the approved comprehensive goal in their explicitly sequenced owning increments (§11 control inventory); Row22 closure is receipt/application-core completion, NOT equivalent-full-payment-form parity or completion of that goal. Recording a payment is recording supplied cash facts, not proof a provider charged or a message was sent.

Local references: approved `notes/active-project-goal.md`; payment draft/boundary; `/tmp/bookflow-payment-family-receipts-feasibility.md`; blueprint 6.5, 10.4, closing-date and ledger contracts; `design/accounting-contract-fixtures.md` fixtures2–6; service-sales, customer-work-and-billing, progress-billing, customer-work-preferences, transaction-deletion and Row20 calculator contracts. Reference evidence W9–W14, printed349 parent/job new-cash grid, printed291 old-credit transfer, printed662–663 independent payment preferences. Supplemental inventory is a completeness cross-check, not permission to fabricate unsupported controls.

## 2. Verified implementation seams

- `company/customer_balances.py` sums actual AR legs per exact name_id and recursively for family. Family totals do not create compatible application capacity. Keep this ledger arithmetic unchanged.
- `company/ledger_schema.py` CHECK `ck_transaction_type` admits journal_entry/invoice/sales_receipt; document envelopes admit journal/sale. `posting_line_sources` has no kind column. Extend actual type/ownership guards; do not invent a source-kind requirement or reuse sales profiles for cash.
- `company/sales.py` owns commercial resolution, fingerprints, immutable revisions, current summaries and Row21 audited-header conflict enrichment. `sales_validation.py` independently recomputes approved effects. Add settlement guards/attribution here, without changing quoted work allocation math.
- `company/document_effects.py` provides exact dated reversal and single-header persistence; a bounded multi-header settlement persister must validate and audit all touched headers in one existing dispatch company write transaction.
- `company/billing_queries.py` currently derives invoice due from gross; replace that projection only. Consumption/proof/source-closing rules remain independent of collection.
- `core/idempotency.py` is actor-scoped, expires after30 days and prunes. `core/dispatch.py` authorizes input before lookup and supports per-command replay. Work conversion permanent keys are separate. Do not describe payment retries as permanent merely by using this cache.
- `company/query.py` cursor watermark is company audit based. `company/ledger_reports.py` transaction-type Literal and register navigation must admit payment. `company/records.py` explicit annotation anchors, custom-field scopes, actual attachments/notes/audit routes need authority propagation, not just a new label.
- Schema head is co13/hub11. Co12's preserving DDL parser/rebuilder is the precedent, not co9's static replacement DDL. Co13 appends preferences; Row22 adds co14, with no new hub capability needed.

## 3. New cash derivation and permanent ownership

Receive inputs express payer plus invoice amounts, never posting buckets. Payer can be any active customer/job. Eligible new-cash invoices belong to that exact party or its current descendants; this recursive rule handles nested jobs consistently with the existing hierarchy. All selected invoices must share the payment AR account and home currency. At receipt, authorize and capture each invoice's actual party and relevant family lineage; a renamed/reparented invoice cannot silently change a previously previewed allocation.

For received positive integer M and positive selected amounts x[i]:

    C[party] = sum(x[i] for selected invoices owned by party)
    R = M - sum(x[i]); require R >= 0
    C[payer] += R

Merge equal party/AR/currency keys; omit zero components. Post one Dr destination M and one Cr AR for each positive C, carrying the exact party ID (`name_type=customer` also for jobs). No revenue or balancing plug. Residual is always the chosen payer's credit; preview and receipt explicitly name that owner. Parent invoices plus residual use one parent component.

After commit, each component's party/AR/currency is immutable. Existing application consumes only the component matching its invoice. Unapply releases credit to that same component. No matching component means no usable credit for that invoice even if the total payment/family balance is positive. A new receipt's calculation never includes prior credits. A payment amount is new recorded receipt money; old-credit application is a distinct action.

Example cents: parent invoice100, A600, B400; receive1000 selecting parent100/A600/B200. Credits parent200/A600/B200; parent residual100. After receipt, exact AR parent=-100/A=0/B=200, family100. Unapply A600 releases A600 without changing AR. Applying it to B is rejected; another A invoice can use it. A separate transfer would require balanced reclassification and is not implemented here.

Single AR account is a justified initial limit: no implicit offset of incompatible control accounts. `ar_account` may be explicitly selected; otherwise resolve the existing system accounts_receivable role. Grid filters to it and explains excluded incompatible invoices. Destination accepts active home-currency bank accounts and the actual system Undeposited Funds account; never accept arbitrary assets as cash. Reparenting affects current family aggregates/pickers, not immutable receipt ownership. Subsequent reuse checks exact component party regardless of later ancestry; historical detail retains captured family facts and separately shows current names.

## 4. Public commands and strict input models

All models forbid unknown fields, floats, booleans as money/versions and explicit null for non-nullable fields. Money is existing exact decimal text or typed `{minor_units,currency}` with consistency checks, home currency only, positive where specified, signed-64-bit persisted bounds and unbounded checked intermediate arithmetic. Dates use existing strict date type. Text/number limits reuse sales conventions (number existing `_Number`; memo2000; payment reference128). Versions are positive integers. Duplicate invoice/application/version IDs reject. Transport pages/chunks have maximum200 (default read page50); that is NOT a receipt/business-intent limit. Initial selections can span arbitrarily many pages through the immutable-versioned selection manifest below, then ONE atomic receive derives ALL owners and posts ONE receipt. Server-derived correction dependencies likewise cover the complete graph. No truncation or splitting an actual remittance into multiple payments.

Every financial payment write, and every applied-invoice correction, requires `operation_key` (ASCII `[A-Za-z0-9][A-Za-z0-9._:-]{0,127}`), plus optional `expected_facts_fingerprint`. Selection-draft commands below are separate audited nonposting preparation and use existing optional30-day transport keys with optional reason. Common reason/directive/on-behalf metadata stays in existing authorized Context, not spoofable input fields. `--dry-run` is available for every write. Payment/invoice corrections, void and recorded unapply require a nonblank reason <=140; receive/apply accept optional reason. Dry run requires the operation key for financial writes so preview and attempted commit share intent, but reserves nothing. Keyless legacy invoice edits retain their existing reason policy.

| Command | Concrete input and behavior |
|---|---|
| `payment receive` | `customer`, `date`, `amount`; optional `payment_method`, `ar_account`, `deposit_to`, `number`, `reference`, `memo`, typed payment `custom_fields`, `custom_field_kinds`; `applications` is tagged `{mode: inline, items: [{invoice,expected_version,amount}]}` (default inline empty; at most200 entries) OR `{mode: selection, selection: ID, expected_version: V}`. The latter resolves the COMPLETE manifest, with no total selection-count cap. Explicit amount at commit is always required and >0; it can originate from the shared calculated draft (§5). Application date equals receipt date. Empty selection means wholly unapplied payer credit. Method omission resolves active effective customer preference, otherwise requires a choice. |
| `payment apply` | `payment`, required `expected_version`, `date`, nonempty applications using the SAME inline|selection tagged model as receive. A selection must have existing_credit mode and matching payment/date. Date >= payment and each invoice date. All selected amounts use compatible existing component capacity. Reapplication after unapply creates a new application with a new operation key, never resurrects the old one. |
| `payment unapply` | `payment`, required `expected_version`, nonempty `applications` of `{application_id,invoice_expected_version}`. Every ID must be an original apply owned by this payment; infer and validate target ID. No date input: exact original application dates. In one command, multiple applications for the same invoice must agree on its expected version. An already-unapplied ID with a new key returns E_APPLICATION_INACTIVE; matching permanent retry replays. |
| `payment update` | `payment`, required `expected_version`; optional ordinary editable date/amount/number/reference/memo/payment_method/deposit_to/custom fields; `invoice_versions=[]` of `{invoice,expected_version}` for all current active targets when an actual correction restates their source allocation. Do not accept customer/ar_account/currency/component amounts or an application selection list. Omitted values retain snapshots; explicit null allowed only to clear optional reference/memo/custom value. Full correction contract §8. |
| `payment void` | `payment`, required `expected_version`. Reject active applications; no auto-unapply flag. Exact old-date reversal, retain original number/revisions/history. Already voided current-version request with a fresh key returns changed=false and is recorded as a durable no-effect operation; old key replays its original effect. |
| `payment show` | `payment`, optional `revision_number`; returns selected historical receipt revision separately from current availability/header. |
| `payment query` | Exact flat names: optional `customer`, `include_descendants=false` (requires customer when true), `component_customer`, `payment_method`, `status=posted\|voided` (omission both), `has_available_credit` (omission both), `date_from`, `date_to` (inclusive accounting dates), `number` (exact), `q` (case-normalized contains over captured number/reference/memo/party display); `sort=date\|number\|received\|unapplied` default date, `direction=asc\|desc` default desc; final binary transaction-ID tie in the same direction; `limit=50`, `cursor`. Combine filters with AND before counts/pagination, including payment_method ID. Current summary only; no implicit aging. |
| `payment history` | `payment`, optional cursor/limit; immutable receipt revisions AND settlement operation events, ordered audit sequence ascending then binary ID ascending, preserving original application, unapply and allocation restatement links. Each entry labels kind and links to exact invoice/payment revisions. |
| `payment invoices` | Flat discriminated model: required `mode=new_receipt\|existing_credit`, `date`; new_receipt requires `customer`, permits `ar_account`, forbids payment; existing_credit requires `payment`, forbids customer/ar_account. Optional `q`, `limit=50`, `cursor`; fixed order invoice date ascending then binary ID ascending. Rows show invoice ID/version/date/number, captured/current party/job names, original/current gross, all-committed due, eligible amount/reason. Filter inaccessible rows before counts/continuation. |
| `payment suggest` | Same flat mode fields plus positive `amount`; `strategy=company\|exact_then_oldest\|none` defaults company. company means exact_then_oldest iff saved automatically_apply_payments is true, otherwise none. Explicit exact_then_oldest invokes it regardless of that preference (the Auto Apply button uses this); none returns no selected rows and full remainder. Compute over the COMPLETE authorized eligible set, not one page; `limit=50,cursor` page the resulting nonzero choices under one audit watermark/recipe hash. Return complete totals/count on every page. Existing-credit choices cap each compatible component. Client collects all pages into a selection draft; counts beyond200 are not errors. |
| `invoice settlement` | `invoice`, optional `as_of` date, cursor/limit for applications. Returns obligation/due, immutable application history links, current versions/capacities and allocation net/tax summary; date-based projection semantics §10. |
| `payment settlement` | `payment`, optional `as_of`, cursor/limit; per-owner received/applied/unapplied components and original/current source provenance, plus target allocations. This is receipt allocation reporting, not a cash-basis financial statement. |
| `payment operation show` | Required `operation_key`; current composite authority on ALL recorded targets before disclosure. Return typed original command, original request envelope/reason/omission data (§9), original immutable effect summary/counts plus current state, including invoice-correction operations. Exact-key lookup, not a fuzzy selector. |
| `payment operation items` | Required operation_key; `kind=request_applications\|effect_applications\|allocations`, limit50/cursor. Pages immutable expanded manifest/effect records and a stable total/hash, not today's rewritten request. Enables exact cross-person recovery of large operations. |
| `application show` | Required `application` exact ID of apply OR unapply. Return immutable row, original apply ID, source payment/component, invoice, dates/amount/provenance, and separately current active/unapplied state with reverse link and current allocation summary. Same composite authority for both endpoints and history. |
| `application history` | Required application exact ID (normalize unapply to its original for chain); limit50/cursor. Ordered audit sequence then binary ID ascending; include original, exact unapply and every allocation inverse/replacement, source/target revision links, total/count. Readonly browser `/application/<id>` and history links use these commands. |

Unspecified read page defaults are limit50 (range1..200), cursor omitted; historical chain order is audit sequence ascending/binary ID ascending. Settlement application rows order effective_date ascending then binary application ID ascending; totals always include the complete authorized set. Query date_from/date_to are inclusive, omitted means unbounded, inverted range rejects. Optional filters are omitted rather than explicit null. Selection IDs and application/operation keys use exact typed identity; normal payment/invoice selectors reuse existing ID-or-number resolution. Header status and settlement state are separate fields; default query includes both header statuses unless filtered.

**Selection drafts separate transport bounds from one receipt's intent.** Add shared commands `payment selection create`, `payment selection update`, `payment selection clear`, `payment selection show`, `payment selection items`, and `payment selection query`. Create takes the same flat mode/customer-or-payment/AR/date fields as payment invoices, optional label(max128); returns selection ID/version1/state open. Update takes selection and expected_version plus `set_items` (0..200 `{invoice,expected_version,amount?}`; omitted amount is unresolved draft intent) and `remove_invoices` (0..200 IDs), disjoint unique IDs, at least one change. Existing items retain ordinal; new occurrences append ordinal, duplicate invoice impossible. Validate references/authority and positive supplied amounts, but unresolved amount is allowed only in draft. Clear takes selection/expected_version and appends one clear marker: no payment/application is unapplied. Show takes selection and optional revision_number; returns immutable version context, state, item_count, manifest_hash, sums and unresolved_count. Items takes selection, optional revision_number, limit50/cursor; order stable ordinal then ID; query takes state=open|consumed (optional), limit50/cursor, newest created first/ID tie. Changing a context requires a new selection; clear/reselect never reassigns existing payment credit.

Draft header has common version/audit fields; immutable selection revisions and item set/remove/clear events preserve every prior manifest. Update/clear are audited nonposting writes with optimistic versions and existing optional generic transport retry. Select/create/clear are normal GUI draft controls, not reservations of financial IDs. Receipt/apply consumes one exact OPEN selection version under the same atomic financial transaction, marks it consumed with a link to operation ID and its own audit entry; a new key cannot consume it again. Matching durable replay precedes consumed-state checks. Keep all consumed manifest history permanently. A new remittance creates a new selection, never splits the original. Inline receive is convenience for small inputs; large clients accumulate bounded selection chunks and then send one constant-size selection reference. Suggestions can be paged into that draft; stale suggestion cursors retain prior selections and require review, not partial receipt. All modes use the same full-manifest authority/capacity/fingerprint validator. `payment apply` also accepts the same inline|selection union (nonempty), with selection mode existing_credit and matching payment/date. Inline no-application receive remains allowed.

Do not create a self-invalidating paging loop: `payment invoices`, `payment suggest` and `payment calculate` cursors authenticate their complete relevant input/candidate/recipe digest, baseline audit watermark and last sort key, not merely equality of the latest company audit seq. Recompute/verify relevant financial/master/permission facts on each page; invoice/payment/version/capacity, relevant hierarchy/default or selected manifest-version changes return E_QUERY_STALE. Unrelated audit writes and updates to a DIFFERENT destination selection draft do not change the candidate/recipe digest, so a client can fetch one suggestion page, append it to its receipt selection, and fetch the next. Calculation against a specific selection revision remains pinned to that immutable revision. Current authority is checked on every page regardless of digest. Other existing query cursors retain their owning audit-watermark contract. Test alternating fetch-and-draft-update across more than200 rows; collecting all pages before one write must not be the only working path.

Add optional `operation_key`, `settlement_versions` to InvoiceUpdateInput. The latter enumerates `{payment,expected_version}`. An applied-invoice correction requires its owner expected_version and either the exact complete related version set OR `settlement_guard`; invoice/payment update accept the guard mutually exclusively with that version list. The guard is a bounded authenticated token, not a bare hash: base64url canonical payload `{v:1,company_id,owner_type,owner_id,owner_version,baseline_audit_seq,issued_at,graph_digest}` plus HMAC-SHA256, maximum2048 bytes. Reuse the existing private company cursor key with a distinct `payment-settlement-guard-v1` domain separator; never expose key material or silently accept unsigned data. Payload contains no unbounded version list. No row reservation or write is needed to issue it.

At baseline seq, the graph digest covers all active application IDs/amounts/dates/component keys, related IDs/header versions and live allocation identities. Under current authority and writer lock, verify token/company/owner, recompute the current graph and independently revalidate before persistence. For mismatch, use the authenticated baseline seq to reconstruct the baseline graph from immutable application/allocation events and owned transaction audit snapshots at/before that seq. Verify the reconstructed digest before claiming a comparison. Compare each related header from its baseline owned revision/version to current and inspect each actual intervening audit event to attribute changed fields to that event's actor/on-behalf/interface/time; report age from event time, separately label latest writer. Return first50 `changes` with exact fields/record/baseline-current versions/event provenance and total/continuation via `payment settlement changes` (input guard, limit50,cursor; authorization and fixed current watermark). Added/removed applications have their exact event provenance. No unrelated pre-baseline event is assigned to the caller's interval.

If baseline snapshots are missing, ambiguous, malformed or cannot verify against digest, reject safely with `unknown_history=true`, affected authorized IDs, unknown fields and any independently known events clearly labelled; never guess an actor/field. Invalid signature/key returns E_PREVIEW_STALE reason invalid_guard without pretending to have a baseline. Valid graph mismatch returns E_PREVIEW_STALE reason settlement_dependencies, attributed comparison, fresh guard and dependency-review link. Old authentic tokens do not expire solely by age; key rotation may require review. Guards are concurrency evidence, never authority; owner expected_version remains required. Genuine403-application correction and actor-interleaving/corrupt-baseline tests are mandatory. Explicit version-list mode retains Row21 baseline semantics and the same truthful attribution requirements.

No caller supplies final allocation cents or ordinals in this initial model. The reviewed exact resolver is mandatory; explicit per-line settlement is a later extension. Old invoice requests without applications and without operation_key retain their existing policy; keyed invoice operations use the durable contract in §9.

Serialize absent new optional invoice input fields as absent, preserving pre-Row22 model_dump and generic retry hashes. Old cached invoice/billing results retain their original output shape; do not mutate saved receipts or rehash work conversion keys.

Register actionable errors explicitly: existing E_VALIDATION/E_AMOUNT_PRECISION/E_VALUE_RANGE for input; E_RECORD_NOT_FOUND and existing authorization errors before facts; E_VERSION_CONFLICT for supplied header tokens; E_PREVIEW_STALE for fingerprints/dependency guards; E_HAS_APPLICATIONS for incompatible date/party/account edits and void dependencies; E_APPLIED_EXCEEDS_TOTAL for edited gross/component floor; new E_APPLICATION_CAPACITY for overapplication with `side=source|target`, exact requested/available Money and authorized record ID; E_APPLICATION_INCOMPATIBLE for exact-party/account/currency mismatch; E_APPLICATION_INACTIVE for a new request against an unapplied original; E_PAYMENT_OPERATION_KEY_REUSED for durable intent mismatch; E_SELECTION_LIMIT only for oversized inline/chunk/page input, NEVER the complete manifest; E_SELECTION_CONSUMED for new attempts to reuse a consumed draft; E_PERIOD_CLOSED, E_INACTIVE_REFERENCE, E_DUPLICATE_NUMBER and bounded E_DB_BUSY retain their meanings. Errors expose current dependency pages and versions only after authority, and retain attempted inputs for UI recovery. No decoder/SQL exception is the public business error.

## 5. Preferences and selection behavior

Append three non-null strict Boolean company preferences across new/show/update and settings forms:

- `automatically_apply_payments=false`;
- `automatically_calculate_payments=false`;
- `use_undeposited_funds_for_payments=true`.

These initialized values are deliberate project defaults, not an assertion about an installation shown in the manual. Omission on update preserves; null/string/0/1 API inputs reject. All three are editable independently under existing company authority, with exact field provenance/conflict behavior.

The first preference controls suggestions in the new-payment form after customer/date/received amount are known. Suggested allocation first chooses one eligible invoice exactly matching the entered amount; ties by invoice accounting date then binary ID. Otherwise walk oldest invoice date then ID, applying min(due, unassigned receipt) until exhausted. Existing-credit suggestions additionally cap by the matching source component. Never mix prior credits with new received money. Save always sends the explicit reviewed list; the core receive command does not auto-select absent applications.

Automatic application is triggered by a user's entered amount/customer change or the explicit Auto Apply action, not by a selection-derived amount recalculation; this prevents a feedback loop that replaces the user's chosen invoices. With auto-apply on and auto-calculate off, algorithmic suggestions still contain exact row amounts; manual selection outside that action requires entered amounts. With auto-apply off and auto-calculate on, manual selections can derive the draft total. Test all four settings combinations, not just separate checkboxes.

**Source adjudication F15:** original local manual printed663 (`manual.txt`30592–30601) describes the normal entered-cash path and per-invoice calculation; printed348 (16541–16548), continuing on349, explicitly describes the ADDITIONAL blank-amount path: with Automatically calculate payments on, first selection fills Payment Amount, further select/deselect recalculates it. Both descriptions stand; neither the original plan's allocation-only interpretation nor an inventory summary implying all typed cash is overwritten is complete. No broad inventory re-review was performed. Parent should replace W9/W10's summary with: “Automatic calculation supports blank Payment Amount derived from selected invoices and calculated per-invoice allocations when received cash is supplied; automatic application independently selects invoices. Explicit cash entry is distinguished from a selection-derived draft total.” This is a requested parent-owned inventory clarification; no inventory file is edited here.

Calculation behavior: with the preference on and amount blank, selecting invoices enters `amount_origin=selection_total`, sets each unspecified row amount to due, and live-sums the selected amounts into Payment Amount; select/deselect and manual per-invoice amount edits recalculate that derived draft total. Display “Calculated from selected invoices” beside it. Typing into Payment Amount switches to `amount_origin=entered`; thereafter invoice selection never overwrites received cash and unspecified selected rows are filled in stable selection order up to min(due, remaining entered amount). Typed row amounts are never clipped; an excessive manual total produces the ordinary mismatch error. Clearing Payment Amount returns to blank/selection-derived mode when auto-calculation is on. With preference off, blank amount prevents allocation completion and requires cash entry; checked rows require manual amounts. Existing-credit calculation uses bounded compatible available credit and never claims new received cash. The normal preview and Save & Close/Save & New confirm the displayed recorded amount; NO extra “accept calculated cash” click. Selecting invoices alone never posts cash. Underpayment remains open due; writeoff/refund is not silently substituted.

Add shared readonly `payment calculate`: same flat context mode as invoices, `amount_mode=company|entered|selection_total` default company, optional amount, and applications inline|selection (inline calculation items permit omitted row amount). company resolves entered when amount supplied, otherwise selection_total iff automatically_calculate_payments is true; otherwise returns a field error requiring amount. Explicit selection_total is a deliberate calculation request and may be invoked regardless of the default preference. entered requires positive amount. Return exact proposed amount, amount_origin, proposed row amounts/origins, due/remainder and complete count/recipe hash; page calculated rows with limit50/cursor for a large manifest. The browser stores complete resolved choices into its draft before financial preview, which always requires explicit resolved amount and no unresolved row. Agents use this same resolver rather than inventing UI arithmetic. Preserve entered values on stale facts; review/repreview replaces only expressly accepted stale suggestions, never automatic invoice substitution. Current invoice selection/deselection is ordinary live arithmetic, not a stale-facts acceptance ceremony.

When UF preference is true and destination omitted, resolve UF; GUI hides the ordinary destination chooser but displays resolved UF in preview. An explicit API destination is still allowed as an intentional override and is shown as such; GUI offers “Choose another destination” to express that override, so the command is not more capable than the human flow. When false, show bank/UF picker and require destination if omitted. Store captured effective choices in receipt profile. Inactive unchanged method snapshots remain renderable/correctable without refresh; new/changed assignments must be active. New financial posting account references must be eligible and active, following existing sale checks.

Fingerprint policy is per command, not whole company version: receive reads UF policy only when destination omitted, effective method chain only when method omitted, selected hierarchy, invoice/payment versions/capacities, exact commercial inputs and custom definition facts. Explicit choices ignore unrelated preference changes. Suggestion output names settings it used; a commit with explicit selections does not depend on an unused auto-apply setting. Existing apply/unapply has no preference dependency. Settings/recommendation change details use audit evidence, never attribute all stale facts to the latest company writer.

## 6. Persistence, type fences and preserving co14 migration

Names below are proposed owning tables, not declarations that they exist. Every immutable table has created provenance and an owned audit event; prohibit update/delete with named triggers. Money rows are positive integer only. Company-local storage is the isolation boundary plus composite ownership FKs.

1. `payment_profiles`: one per payment revision; typed transaction_id/revision_id with payment-type composite ownership, payer ID and captured identity/address/lineage facts, AR/destination account IDs+snapshots, method+snapshot, reference, captured effective preferences. Revision retains date/number/memo/gross and typed custom snapshot like existing documents. One `document_line_identities` stable business line and one `document_lines` envelope kind `payment` per receipt revision. Kind payment has no journal side/account/amount or sale-profile values; its amount comes from receipt revision/component facts.
2. `payment_component_keys`: immutable ID, payment transaction ID, stable receipt-line ID, exact party ID, AR account ID, currency. Unique(payment,party,AR,currency), owned stable-line FK; immutable dimensions. `payment_components`: immutable revision-local ID, stable key ID, transaction/revision/document-line ownership, positive capacity amount, captured party/account facts; unique(revision,key), compound uniqueness for attribution FKs. Current zero capacity means absent component, never a zero row; key persists.
3. Append nullable `payment_component_id` to `posting_line_sources`, referencing the exact owned payment revision component. Payment sources require it and tax_component_id null; other document types require it null. No new source-kind column. One cash leg can have one source row per component; each AR leg has that component's source. Each component appears once in cash attribution and once in exact-party AR attribution, each side summing to its capacity. Exact reversals preserve both links. Extend typed source output optionally, omitting this new field for old nonpayment rows/replay serialization.
4. `applications`: immutable id, kind apply/unapply, paying_transaction_id constrained payment, paid_transaction_id constrained invoice, source_component_key_id, amount_minor_units/currency, effective_date, recorded_at, unique nullable reverses_application_id, audit event. Original apply has no reverse reference; unapply copies exact endpoints/key/currency/amount/date and references one original apply. Enforce same owned source and typed target, inverse equality and one unapply per original. No update-in-place active flag.
5. `settlement_line_keys`: immutable transaction_id, stable invoice line_id, positive ordinal, created/audit provenance. Unique(transaction,line) and (transaction,ordinal), owned identity FK. Keep retired keys. At first application assign keys to all current invoice lines in binary stable-ID order; afterward retain and assign above historic max to submitted new occurrences in order whenever an invoice with keys adds lines, even when it currently has no active applications.
6. `application_allocations`: immutable id, original apply ID, kind allocation/reversal, unique nullable reverses_allocation_id, effective_date, recorded_at, audit event, positive amount/currency. Include owned source payment revision/component and exact AR posting-source link, target invoice revision/envelope/stable line/ordinal, logical kind net|tax and tax_item_id-or-null, physical tax_component_id-or-null, exact target AR attribution and income/tax posting attribution, recognition_role sales_net|tax_liability, semantic component facts snapshot. Composite FKs reject unrelated revisions/lines/sources. Reversal copies all original business attribution/amount/date and references it; replacement may update source/target attribution but retains original application identity. Net allocations have no tax ID; tax allocations require the matching component. Sales tax is never income. An inverse references an allocation, never another inverse. Validator ensures net effective sum equals each active application's amount, zero for unapplied.
7. `payment_operations`: permanent immutable successful-operation receipt, also covering keyed invoice updates. Company-local operation_key is unique across ALL participating verbs including invoice update; command, canonical request version/hash, exact replayable request envelope, resolved owned IDs/expanded-manifest hash, original typed effect, NON-NULL owned audit event and timestamp. Every first successful no-financial-effect update/void records a separate attributable operation audit event/entry with the current actor/interface/on-behalf/reason and `financial_changed=false`, plus its permanent receipt. It does NOT bump financial headers or create a revision/posting/application. Thus every durable execution is visible in ordinary authorized audit/activity, even when its financial effect is none. Repeated matching replay and previews insert nothing, including no audit heartbeat. An audit failure rolls back the operation receipt. Large operation request/effect item pages use owned immutable operation-item rows, not an oversized unbounded HTTP response.
8. `payment_selections`, `payment_selection_revisions`, `payment_selection_items`: nonposting preparation header with open/consumed state, version, context and current revision; immutable revision/audit records and ordinal set/remove/clear item events. A revision identifies a complete reproducible manifest; do not delete older items or cap total member count at a page size. Composite FKs enforce selection/version ownership; consumed header binds the successful operation. Creation/update/clear/consumption are audited under the same composite rules. Their new table/record types and integer/version/state guards belong in co14 and current metadata. No financial key/ID is reserved by a draft.

Migration co14 extends transactions' type CHECK to payment, document-line kind constraints/type-insert trigger, posting-source component ownership/nullability guards, and appends three info preferences plus one source column. Creates new tables/indices/immutable and inverse guards. No hub migration: payment reads use ledger.read, writes ledger.post plus existing linked-work rules. No old documents/components are backfilled; old invoices acquire settlement keys only in an authorized later write.

Use co12's schema-aware preserving approach: parse exact recognized named constraints while retaining unknown columns/DDL; append columns before table constraints, preserve existing column prefix, rowid, typeof/quote/raw bytes including generated values, indexes/views/triggers and attachments. Capture/recreate local dependency objects in safe order. Fail atomically for unsupported WITHOUT ROWID/shadowed rowid, ambiguous constraint shapes, unknown conflicting type guards, preexisting reserved table/column names or violations. Do not silently drop local extensions or rebuild from static SQLAlchemy metadata. Validate every existing column/raw row, original objects and attachments, and foreign_key_check; no version/audit mutation of old business records. Heads/new-book metadata and migrated DDL converge. Upgrade runs through documented `bookflow upgrade --data-root <disposable> --reason ... --json`, not hand-edited hub SQL. A historical co13 fixture remains pinned to co13; do not replace meaningful failure expectations with current-head success.

Independent DB guards cover type/ownership/positive/inverse invariants; independent service validator checks aggregate capacity, exact batch/source allocation, both-date locks, complete version sets, derived family split and operation receipt. Neither foreign keys nor planner assertions alone prove these totals. Re-read records under the existing serialized company writer transaction; bounded E_DB_BUSY is allowed, then same losing request must fail E_VERSION_CONFLICT after the winner. Persist multiple headers+financial/application rows+audit+operation receipt atomically; injected failures anywhere leave all business/operation state unchanged.

## 7. Deterministic allocation and restatement

For target invoice, eligible components are every positive STORED net and positive stored per-line tax component for the complete supported nonstock family (§1), including amount-priced and fractional progress-allocated profiles. Zero components remain commercial facts but receive no monetary allocation row. No quantity/unit-price reconstruction or service-item-only filter. Current tax groups require distinct tax items, so logical component key is `(settlement line ordinal, kind 0 net / 1 tax, binary tax_item_id or empty)`. Use physical IDs only as provenance. Account/agency/rate/taxable/net facts are part of semantic identity comparison, not tie order; invoice line display order, regenerated row IDs and unrelated display-only labels are not.

For requested A, remaining capacities c and T=sum(c): q=(A*c)//T, r=(A*c)%T. Award A-sum(q) cents by descending r then logical key. No floats/rounding acceptance/zero rows. Full settlement consumes exact residue. Normal apply uses all existing committed active allocation capacity, including future effective dates, and never reshuffles prior applications merely because the new date is earlier. Any historical projection must still have nonnegative per-component capacities under the resulting dates; reject incompatible date/capacity input instead of recomputing old allocations silently.

**Owning oracle amendment before code (F14):** parent has amended BOTH blueprint10.4 and `accounting-contract-fixtures.md` Shared checks to say equal remainders are ordered by `(durable settlement ordinal, net-before-tax kind, binary tax-item ID)`, not arbitrary physical/stable ULID order. This is the explicit semantic replacement; the old conflicting fixture text is not retained as an alternative. Independent fixture: retained line ordinal1 with binary ID Z and newly added ordinal2 with binary ID A, equal remaining net1/1, payment1 -> ordinal1 receives1, ordinal2 receives0 despite A sorting first. Within one line net1/tax1 and payment1 -> net1/tax0. For net100/tax8/tax8 with tax-item IDs A/Z, payment8 -> net7/tax-A1/tax-Z0; final payment108 consumes93/7/8, regardless of physical tax-component ID order. These hand-fixed expected cents live in the owning fixture, not the allocator under test. Fixtures2–3 numerical oracles remain unchanged. No existing payment allocations require migration because payments are not implemented at co13. Preview, commit, correction, reordering and later residue tests all use this same authoritative rule.

Preview keys for retained lines are saved ordinals. New line occurrences get deterministic provisional ordinals above historic max in submitted order, including duplicate identical lines. Commit independently resolves the same occurrence recipe, allocates fresh ULIDs and attaches those ordinals; no ID reservation/cache reuse/deterministic-ULID hack. Fingerprint includes canonical intent, owned current versions, active applications and component facts/capacities, and the resulting logical allocation recipe, excluding transient row IDs/event IDs/timestamps. Key IDs created on first application/new component similarly map from deterministic semantic keys. A fingerprint verified on two previews and commit must predict exact cents by logical component.

On applied invoice semantic edit, recompute the COMPLETE surviving applications in `(effective_date, binary application_id)` order against the revised component capacities. Compare each proposed full split/facts with its live allocation set. Changed applications append exact inverse of all old live allocation rows and full replacement; unchanged applications retain allocation rows, even if they reference an older physical invoice revision. Current capacity follows stable logical keys, never only current_revision_id. Display current provenance separately from retained historical effect. Adding/removing/repricing lines or changing tax rules can change the denominator for otherwise unchanged lines, so compare complete splits, not just edited component IDs. Reordering retained lines alone preserves keys and splits.

Every changed/restated application keeps original ID, amount, source component, effective date; application rows themselves are not reinserted. Check every original allocation date against closing date. Missing/ambiguous physical attribution is validation failure, never invented revenue. Payment correction (§8) restates source provenance without recalculating the target split, avoiding propagation to unrelated payments on the same invoice. Later unapply reverses the exact CURRENT live allocation rows, not a fresh computation of the original split.

## 8. Corrections, versions and conflict diagnostics

Header versions are concurrency tokens, not commercial revision numbers. Each command bumps each affected payment/invoice header once, with a full attributed transaction audit before/after snapshot and ordinary update action; commercial revision increments only when commercial receipt/invoice content changes. Apply/unapply changes no document revision, no GL batch and no work header. Receive payment starts version1 and increments each selected invoice once. An invoice correction plus its allocation restatement is one invoice bump, and each payment whose allocation changes bumps once. Payment correction plus restatement is one payment bump, each affected invoice once. First-time no-financial-change executions preserve headers/revisions/GL/application rows but DO record their own operation receipt and attributable audit event (§6). True replay creates neither.

Row21 conflict lookup remains audit-header based and conservative. Extend sale/payment enrichment with **actual attributed settlement history**: report `settlement.applied`/`settlement.allocations` when corresponding application/attribution events occurred since a validated expected header; include event IDs and actor/on-behalf/interface/time in a separate settlement_changes collection. Preserve latest-writer wording and unknown/future history; never infer settlement from a bare version bump. Missing history still reports unknown. A stale invoice commercial editor seeing a new payment must reject and retain its draft, even when the commercial revision is unchanged. New payment commands require versions; unaffiliated legacy sale blind policy is unchanged.

Invoice edit: positive gross must cover all committed active applications; party/AR/currency unchanged while applications survive, and new date cannot exceed earliest application date. Fail E_APPLIED_EXCEEDS_TOTAL or E_HAS_APPLICATIONS. Existing old/new posting-date locks remain. Existing work-linked line/proof/receipt invariants remain before and independently inside validation. For actual changes requiring settlement recomputation, require `settlement_versions` covering exactly every active source payment OR the complete `settlement_guard`; authorize all first. If recomputation finds a payment unchanged, validate its version through the selected mechanism but do not bump it. The complete set prevents a phantom new application between preview/commit. A truly identical invoice no-op needs no additional payment tokens and preserves ordinary behavior.

Payment lifecycle is explicit: update/apply require posted; update of a voided payment rejects E_VALIDATION with state=voided before planning replacement effects, even for memo/reference and even if input would be a no-op. Apply of voided payment rejects E_APPLICATION_INACTIVE. Unapply requires posted source and an active original application; voided-with-active-application is invalid stored state, never permission to revive it. Void accepts posted with zero active applications; void on already voided with correct expected_version is a first-time audited no-financial-effect operation or matching replay. Reads and authorized historical replay remain allowed in either state. No operation revives a voided receipt or reverses its original batch twice.

Payment update is useful but explicitly bounded:

- Editable number/reference/memo, payment method, destination, accounting date, received amount and typed custom fields. Keep payer, AR, currency and non-payer component amounts fixed. An accidental different payer/job split requires explicit unapply → void erroneous receipt → receive corrected record with a new key/reason; closed dates may prohibit it. This is a correction workflow, not an advertised transfer of valid existing credit.
- Amount delta changes only the payer component. Its resulting amount must be nonnegative and cover its own active applications; total must remain positive. Example parent receipt with A600/B200 and parent200 can decrease to900 if parent applications<=100, but cannot decrease by borrowing A's released credit. A payer-only receipt can freely increase/decrease subject to its applied floor. Increasing old recorded cash is a correction; another remittance uses receive. This protects immutable job ownership without forcing ordinary memo/date/bank/amount fixes through void.
- Preserve old snapshots unless a changed method/destination/custom definition must resolve; captured payer identity can refresh only via a separately explicit future mechanism, not an accidental current-master rename. Date must remain <= every application date and old/new accounting dates open.
- Follow existing immutable sale correction shape for any actual receipt content revision: exact old batch reversal plus new balanced replacement, real revised receipt facts, same stable source keys. This is a real commercial correction, never a revision created solely for settlement version alignment. Changed source physical attribution causes full inverse/replacement allocation attribution for active applications at original dates, retaining their target splits and IDs. Require exactly all active target invoice versions OR the complete settlement_guard, authorize them and bump each once. If no active applications, no invoice tokens. Independently validate source/target totals and ownership after correction. A no-op creates no commercial revision/batch.

Invoice void rejects active applications. Payment void rejects active applications; explicit unapply first is required, then exact old-date inverse. Unapply uses original dates, not a convenient current date. Matching durable replay of any prior operation runs before current eligibility/version/closing-date checks (after current authority), so it remains inspectable after void/closure/settings changes and performs no new effect. A new operation key requests a new action and cannot bypass current restrictions.

## 9. Permanent operations versus ordinary retries

All financial payment writes and applied-invoice corrections require durable operation_key, company-wide across participating verbs, actor-independent so an authorized human and program can safely share the SAME intent. Invoice update accepts optional operation_key for any invoice and REQUIRES it when the current invoice has active applications and an update is requested; check authorized saved operation first, even if its invoice is now unapplied/voided. This namespace is explicit, including command=invoice update; no silent reuse of a payment-receive key. Current authorization is always applied to saved owned targets before replay/mismatch details; possession of a key grants nothing. A caller without required linked-work access gets uniform denial, not existence, balances or previous actor data.

Canonical request version1 hashes validated explicit input with defaults represented as omissions, command and selected company, and supplied reason (including absence versus supplied value). Normalize exact money and stable syntax; preserve ordered lists where occurrence order is meaningful. Exclude expected_facts_fingerprint and operation_key itself; INCLUDE requested expected versions, original selectors, authenticated guard string when supplied, manifest ID/version/hash and all business changes. Store an executable `request` envelope `{request_schema_version,command,input,provided_fields,context:{reason},context_provided_fields,canonical_hash,resolved_refs,expanded_selection_hash}`. Input is the accepted original structured request including operation_key, with exact omission distinctions, recoverable old selectors/versions and optional original preview fingerprint. Execution actor/on-behalf/interface remain separate immutable provenance and are never credentials/authority to impersonate on retry. New caller uses its own current authorized Context; stored reason can be resubmitted as returned. No tokens/secrets are stored in the envelope.

`payment operation show` returns this envelope after composite authorization, including keyed invoice corrections; `payment operation items` recovers expanded manifest members/effects at the immutable original version even after the selection is consumed. Stored manifest versions cannot be garbage-collected out from under permanent recovery. A caller with only operation_key can retrieve, inspect and replay the original command/input/reason without guessing aliases/defaults/versions. Type-specific historical schema version1 remains accepted for replay after later model evolution. Matching replay uses saved IDs for authority rather than re-resolving renamed original selectors/defaults. A changed intent under the same key returns E_PAYMENT_OPERATION_KEY_REUSED without effects. Failed validation, stale preview and preview reserve no key; a corrected draft may reuse it until first success. After success, edits require a new key.

Permanent lookup belongs in input authorization (to select saved owned targets safely), in normal planner before fresh-fact resolution, and in validator/persister under lock. Replay returns immutable original `effect` plus newly authorized `current`, with `idempotent_replay=true`, `changed=false`, `new_effect=false`; it never rewrites the operation receipt. Original effect has `changed` describing the original operation separately. Previewing a replay says “Already recorded; this retry creates no new payment/application/correction.” Current state can be voided/unapplied; do not render a future effect from the original receipt.

Payment commands and keyed invoice updates may additionally accept the existing optional transport idempotency header. Its actor-scoped30-day behavior remains, including full transport-request mismatch. Replay hook always reconstructs through the permanent operation rather than cached current balances. Different payload under one transport key may still return E_IDEMPOTENCY_MISMATCH; exact transport retry retains the request. Durable retry may omit that header and use recovered original command/input/reason. After generic expiry/pruning, applied-invoice correction MUST return its original successful immutable correction receipt plus current state, not merely a safe stale-version rejection. Keyless legacy invoice updates without applications retain their prior30-day behavior and old serialization; existing work permanent conversion hashes/namespaces are unchanged. Tests must lose an applied-invoice response, prune generic cache, rename masters, change actor and unapply/void later, then recover by key and replay without a second correction.

Race uniqueness applies at database level; if two authorized actors use one operation key, only one effect/receipt may commit. Loser re-reads and returns matching original/current result or mismatch. No shared mutable in-progress receipt or unbounded polling; normal bounded DB lock errors remain retriable with the exact key. Validate cross-interface and >30day retries, after-unapply/void replay, same key different command/amount/target, lost response, and process interruption between every persistence stage.

## 10. Typed outputs, current and effective-date queries

Every payment write returns:

- `operation_key`, `operation_id`, command, `dry_run`, `idempotent_replay`, `changed`, `new_effect`, warnings, facts_fingerprint;
- immutable `effect`: original header/revision IDs and versions before/after, receipt amount/date/number, selected invoice amounts, source owners, original application/allocation IDs and logical recipe, exact monetary deltas, audit event/provenance, and original preference/default facts;
- live/prospective `current`: payment header/version/status, selected commercial revision ID/number, received/applied/unapplied Money values and per-component ownership/capacity, current invoice gross/due/version/settlement status for touched targets, and current record links.

Preview new physical IDs are tentative and labelled so; logical recipe is the promised cents. Real execution returns durable IDs. `effect` is a typed discriminated receive/apply/unapply/payment_update/invoice_update/void union, not an unvalidated generic dict. Large results return complete totals and counts, first50 items and immutable operation-item/current-query continuation descriptors; count/page is never the business selection count. Read results have separate historical revision/effect and current fields. Snapshot facts retain original master names; current references may show current names distinctly. Original executable request is recovered via operation show/items, not reverse-engineered from effects.

Invoice current settlement = gross, active applied, due and state unpaid|partial|paid; voided uses state voided and zero due, never claims cash received. Payment current received is zero available effect if voided, while selected historical revision still reports original received amount. Payment state unapplied|partial|fully_applied|voided is separate from posted/voided header. Per-component amounts sum to payment totals. Sales receipt remains paid-at-sale, not an invoice with a fictitious application.

Live invoice show/query and billing destination due use one shared all-committed settlement projection; add optional typed settlement_current fields to sales outputs without altering historical revision totals. New read projections populate it; preexisting stored retry receipts without it remain compatible and direct clients to invoice settlement for current data. Preserve work `source_effect`/`source_current`, receipt confirmation and scope proof outputs. Payment and application show/history, invoice detail, billing panel, register and customer detail link to the same records.

`as_of` means **effective-date cutoff using all currently recorded corrections**, not “what the user knew then.” Return cutoff, projection_basis, company audit watermark and generated_at. Derive invoice obligation and payment per-component credit from their dated original/reversal/replacement posting effects <=cutoff, then net effective apply/unapply rows <=cutoff and allocation inverses/replacements. Do not select today's active revision merely because its header is current; dated batches determine effective obligation, and logical component mapping retains valid historical references. A backdated unapply corrects its original date, so a later rerun can differ; original exported/rendered results keep their old watermark. Recorded-time history remains inspectable through audit/history, but bitemporal `recorded_as_of` reconstruction is not promised in Row22.

All-committed editable capacity includes future transactions/applications and is explicitly labelled. `as_of` projections are read-only and never used to authorize capacity. Current customer AR/family balance remains the posting-derived value and need not equal the sum of currently visible invoice due alone because unapplied credit exists. Receipt allocation view separates sales net from tax settlement and never pretends to be full cash-basis P&L. Existing unsupported cash basis request must still reject.

Ordinary live query pagination uses authenticated audit-watermark cursors over fixed filters/cutoff/sorts. Payment invoice/suggestion/calculation preparation is the explicit relevant-digest exception in §4 so draft writes cannot invalidate their own page traversal; actual relevant financial changes do invalidate it. Immutable operation/selection-version item pages bind immutable manifest hashes and remain readable after later writes. No N+1 unbounded per-invoice history walk; aggregate applications with indexed source/target/date/reversal keys. UI pages/chunk writes are200 maximum, total initial selection is not capped: one complete versioned manifest feeds one atomic receipt. Correction dependencies likewise process the complete graph in bounded database fetch chunks inside ONE transaction, using authenticated settlement_guard when the caller does not enumerate versions. More than200 active applications must not force unapply (which may be closed) or split a correction. Require genuine201-distinct-job and403-distinct-job ONE-remittance witnesses plus a genuine403-application correction/preview witness, exact baseline diagnostics and atomic rollback. Test another non-round count to prove page-size independence. If measured runtime exceeds the command budget, flag a long-loop performance issue and optimize the aggregate path; never silently truncate or manufacture partial accounting. Selection creation/updates being separate preparation transactions does not relax receive's single atomic posting/application boundary.

## 11. Authority, custom fields, evidence and recovery

Reads require current selected-company ledger.read; writes require standard ledger.post. Work-linked invoice read/member and write/standard requirements match `billing_queries.authorize_sale`, including historical linkage after unapply/removal. A payment with ANY historical application to a work-linked invoice inherits that read/write requirement for its detail/history/replay/correction; do not shed authority when unapplied. Receive/apply explicitly authorize every target before versions/capacities. Query filtering must not leak hidden row counts, suggestions or aggregates: either filter accessible payment/invoice rows with consistent totals or require the resource for the requested aggregate; choose filtered list queries and resource-required direct family settlement aggregates. Error endpoints preserve this ordering.

Register payment/profile/application/allocations/operation/selection audit types explicitly. Payment custom values implement ALL established kinds: text, exact number, date, Boolean and choice, using the owning custom-field validation/precision/date/choice contract, active payment-scoped definitions, inherited defaults and required checks. On create resolve defaults only for omitted values; on update omission preserves captured value, explicit clear follows established nullable/required rules, and wrong expected kind rejects. Inactive definitions/choices remain historical facts; new/changed assignments obey existing eligibility rules. Historical revision snapshots retain typed value and definition/choice facts; never coerce to text/bool or ignore a required number/date/choice. Shared custom editor and generated CLI/HTTP/MCP schemas must expose the same kinds. Tests create required definitions of every kind, default/clear/preserve, definition edits and historical replay.

ONE composite disclosure rule applies to payment/selection/application/operation detail, audit events AND entries, before/after snapshots, activity summaries, attachments/notes/evidence, printable views, raw-download metadata and direct URLs: caller must pass existing surface capability PLUS the full document composite authority (ledger and historical linked-work resources). Traverse typed owned references, with cycle protection, before rendering counts, amounts, names, hashes, request envelopes or links. Generic company audit authority is NOT a bypass. For a mixed audit/activity event containing any inaccessible protected document, omit the WHOLE event from list/count/pagination and deny its direct lookup; do not reveal its unredacted summary or safe-looking sibling entries. Entry direct lookup enforces its owning event rule. Evidence attached to mixed-protected endpoints requires all endpoints' authority. Missing/ambiguous ownership fails closed. Filter before pagination/totals. Existing unrelated audit records retain their ordinary policy; this is a bounded payment/settlement ownership extension to those surfaces, with actual bypass tests rather than a documentation-only exception.

Payment print view is an internal receipt with original received facts and labelled current settlement, not delivery/provider confirmation. Typed application show/history is the common inspection path for all application links. Payment-method/date query controls are present in the payment list and filter before pagination.

Browser receive form: customer/job selector, received amount, date, method/reference, bank/UF choice, exact/family AR labels, paged invoice grid (checkbox,date,job,number,original,current,due,payment), applied/unapplied totals with owner, preview then save. Keyboard/screen-reader labels and tab order; Row20 metadata enables calculator arithmetic on amount fields, numeric Enter remains calculator-only. At390 columns become labelled rows/cards with contained history scrolling, not lost amounts. No raw posting component editors.

After a definitive stale/version/capacity error retain all typed fields, selections and operation key; show saved/current comparisons and separate latest-writer/settlement event provenance. Never silently clip amounts, change customer/AR, substitute another invoice or turn on a preference. Offer explicit “Review current amounts” and “Use these suggestions” actions, then repreview. Missing dependency versions are recovered by a visible review action that retains commercial draft. After ambiguous transport failure preserve EXACT submitted request and keys until replay resolves; do not allow a second key to create the same apparent payment accidentally. Successful receipt stays visible; current balances refresh separately.

Show/apply/unapply/update/void are full GUI flows using the same models, including required version sets and unapply-before-void guidance. Human can use an agent-created payment's operation/history links without source knowledge; agent can read and preview correction of a GUI-created payment through discovery/schema and ordinary typed selectors.

Ordinary controls are in scope: Save & Close saves then shows receipt/detail; Save & New uses the SAME save operation and only after confirmed success starts a new key/draft, retaining customer/date/method/destination while clearing received amount, selections, reference/memo and resolving new custom defaults. Ambiguous save never starts a new key. Auto Apply uses explicit exact_then_oldest and the complete paged recipe. Draft Un-Apply/Clear selections deselects the whole manifest through selection clear; it is explicitly distinguished from unapplying a recorded application and makes no GL/application changes. Selecting/deselecting invoice rows and manual per-row edits are ordinary draft actions, with live exact calculation as §5. No dummy disabled controls stand in for these functions.

### Required control-by-control continuation inventory (F17)

The approved goal explicitly orders payments before dependent credits/refunds/banking/provider work. The following are REQUIRED follow-up work, not a request to reapprove that goal and not claims of full reference-form parity at receipt-core closure. Parent carries each unresolved control into the next owning plan/inventory until implemented; only the user may defer the comprehensive goal's required scope.

| Reference control | Row22 behavior / required owning follow-up |
|---|---|
| Receive cash; payer/jobs; date/method/reference/destination; amounts/grid; Save & Close/New; draft Auto Apply/Un-Apply; print receipt | Fully implemented here, including blank-derived and entered calculation paths, complete multi-page single receipt and query filters. |
| Available/previously applied credits, selected amounts, credits used/remaining | Row22 labels **payment-source credit** explicitly, supplies per-owner available amounts, application show/history and reuse/unapply through the same UI/commands. A credit-only apply does not create new cash. Required customer-credit-memo increment adds credit-memo sources and unified Available/Previously Applied Credits chooser; AR-tagged journal credit is not fabricated as a payment source. |
| Grid Credits column and combined cash/credits-applied totals | Current payment-source apply view shows applied payment credit separately. Required customer-credit/allocation integration adds explicitly selected old sources alongside NEW cash in one coordinated receive form, with exact ownership and no implicit transfer. Do not claim that separate Row22 flows already match this combined control. |
| Discount date; suggested/chosen discount; account/class; resulting balance; discount grid column and combined totals | Required customer settlement discounts/writeoffs increment: own typed components, date eligibility, independently balanced postings, account/class and quote-versus-executed output. Row22 leaves unpaid due and displays no fake discount amounts. |
| Overpayment retain credit versus refund | Retain exact-owner payment credit works here. Refund choice is required customer refund document/settlement lifecycle next; no fake refund action or cash movement now. |
| Underpayment keep balance versus writeoff | Keep remaining invoice due works here. Explicit writeoff operation with chosen account/class and audit is required with discounts/writeoffs; never rounding or automatic cash inflation. |
| Advance/prepayment liability versus unapplied AR credit | Unapplied AR credit is labelled precisely. Required customer advances/deposits increment introduces liability-held documents, application/refund and tax/basis policy; a payment-receipt label must not disguise that obligation. |
| Cross-job OLD-credit transfer | New cash family allocation works here. Explicit balanced old-credit transfer remains required customer-credit integration; preserve separate original source ownership and traceable reclassification, never rederive a saved receipt's jobs. |
| Deposit to bank/UF versus bank deposit grouping, reconciliation and bounced check | Receipt destination works here. Required banking/deposits/reconciliation/returned-payment plans provide document dependencies, deposit selection/redeposit guards, reversal/discrepancy policies and exact effects. |
| Delete, find/history, attachments, payment-method/date correction lookup | Find/history/evidence/filter/correction are implemented here. Separately permissioned Delete remains required under accepted deletion contract and the invariants below; no physical-erasure shortcut. |
| Process payment, raw card/expiry, downloaded/provider receipt, external delivery | Required separately scoped provider/delivery integrations use protected references, explicit authority and actual outcomes. Raw credentials/implicit external actions are prohibited design departures, not missing fields to reproduce. |
| Aging/statements/full cash-basis and payment-receipt tax-liability policy | Receipt allocation/current/as-of views here are labelled limited. Required report/tax-policy owning increments complete declared recognition and history coverage; do not label this data view a complete financial basis. |

### Future Delete invariants (F19)

Deletion is a separately authorized business state, never removal of headers, revisions, component keys, applications, allocations, selection manifests, operation receipts, evidence or audit. A future posted-payment/invoice delete must reject active applications or use an explicitly reviewed atomic dependent reversal; it must honor every original date, exact inverse uniqueness and version constraint. Already voided deletion cannot add another reversal. Ordinary-list hiding must not hide required dependency discovery or destroy exact-owner available credit; unresolved live credit/deposits/reconciliation must be explicit dependencies, not stranded off-list balances. Historical/deleted detail and operation/application reads remain available to authorized readers; current outputs label deleted when that state exists, while original effect stays unchanged. Permanent keys remain occupied; retry returns original effect and current deleted state, never recreates cash or revives a record. New apply cannot target a deleted/ineligible source or invoice. Composite disclosure and evidence links survive list hiding; future restoration requires its own explicit contract, not automatic replay. Row22 does not add Delete or invent deletion capabilities.

## 12. Tests, seeds, docs and review gates

Meaningful independent financial oracles:

1. Fixtures2–6 with supported service analogue: taxable service10000+tax800;5401 -> net5001/tax400; final5399 ->4999/400; applied edit adds exempt2000 ->4219/338/844; final7399 ->5781/462/1156. Assert exact source/target attribution, gross/due/credit, full ledger and no payment revenue. Explicitly omit inventory/COGS assertions outside implemented service scope; do not claim full original inventory fixture passes.
2. Parent/job scenario §3; prior unrelated credit untouched; parent residual cannot fund job; unapply A and apply another A invoice succeeds without GL; B fails. Nested hierarchy/reparent stale preview; exact component AR-account isolation; component amount-floor correction; useful memo/date/method/bank/amount edits; zero retired payer component with no zero leg; void every party exact.
3. Invoice line add/reorder/remove/duplicate-identical/tax component add/remove/rate/account change; equal remainders spanning retained and provisional new keys. Multiple previews and commit generate different physical IDs but exact same logical cents/fingerprint; later installments and chronological restatement preserve totals. Mutation tests for sorting regenerated IDs, filtering only current revision, pooling family credit, and rounding with floats must fail independent oracles.
4. Closing period: original May payment/unapply/edit reject when closed; new June receipt/apply on old invoice succeeds; as-of May31/June2 reconciles; future committed applications prevent current double-spending; void/reversal inclusion and backdated correction rerun watermark explicit.
5. Both-header and multi-header concurrency; one winner, bounded busy then same losing request conflict, no duplicate/overapply. Failure injection after header/audit/component/app/allocation/operation receipt inserts and custom/sequence steps proves transaction rollback. Rejection and dry-run raw snapshots contain no new rows/versions/audit/keys. Permanent replay after cache pruning, actor change with authority, rename/inactivity, unapply/void and lost-response interruption writes nothing. Unknown/malformed Row21 history remains safe.
6. Actual CLI JSON and authenticated HTTP discovery/preview/execute/read/retry/denial; HTTP receives multi-job cash, browser1280/390 inspects and unapplies/reuses same-job credit, CLI verifies exact unchanged posting balances and actor provenance. Reverse journey: browser receives, HTTP previews applied invoice correction with authenticated dependency baseline, human sees original/current history and attribution. Before Row22 closure execute actual Row9 MCP discovery, authenticated receive/apply/correction/recovery and denial, continue its records in GUI and reverse direction, compare typed values/errors/provenance with CLI/HTTP. No mocked MCP result or registry-only check substitutes for that gate. Readonly/revoked/foreign-company/work-link denial must include direct audit entries/events, mixed-event snapshots/activity, attachments, selections, recovered requests and operation-item pages.
7. Co13→co14 disposable preserving upgrade via CLI with local columns (including generated/BLOB/text storage), rowids, custom index/trigger/view, attachments and audit histories. Compare every old column byte/storageclass/prefix and object definition except exact declared constraint/append changes, old info fields unchanged, hub revision only publication metadata. Unsupported shapes fail with restored original database. Fresh head and migrated head typed schema/guards agree. Raw insertion attacks attempt foreign revisions/sources, duplicate inverse, wrong parties, wrong type, zero/noninteger amounts and altered immutable rows.
8. Fresh company/upgraded preferences defaults, invalid Boolean values, all four auto-apply/calculate combinations. Blank calculated amount follows select/deselect; manually entered amount stays fixed; selecting invoices never posts until ordinary preview/save; no redundant acceptance click. Mixed service/noninventory/fixed-charge/unit/amount/progress invoice profiles use stored amounts. All custom kinds/defaults/required/clear/history; payment-method filter before pagination; posted/voided update gates; Save & New and draft clear; exact application reads. First-time no-effect execution has its own audit event but unchanged financial rows/versions; replay writes nothing.201/403/non-round large remittances use one receipt across multiple selection chunks with no global count cap; whole-graph403 correction includes signed baseline, two intervening actors, real field/time attribution, missing/corrupt audit unknowns and failure rollback. Generate docs/schema and validate1280/390 states. Existing Row21, work billing/progress/preferences and relevant register/report/source attribution regressions remain. Do not duplicate an unchanged full suite during implementation; integration owner chooses final broad regression.

Seeds: append only to frozen existing seed.toml295 commands and reference.toml201 (Row19 appended12 to283/189). Preserve exact byte prefixes/command objects, all old document/application/audit rows, old keys/numbers and old customer balances. NEW active examples deliberately change aggregate ledger totals; derive the increment independently, do not cancel all examples to conceal it. Add Payment Example Customer and jobs A/B, isolated Payment Example Income and bank accounts, exempt supported item, and DEMO-PAY-/REF-PAY- identities. The same per-company cents oracle is:

| Final active example | Exact facts |
|---|---|
| Invoices parent2000, A10000, B6000 | Total18000 net revenue, no tax; all posted. |
| P1 parent-family receipt | Initially12500, applied parent1000/A8000/B3000 (12000 total), residual parent500. Correct received amount to13000: only parent component increases by500, final components parent2000/A8000/B3000. Explicitly unapply then reapply A8000 to demonstrate same-owner reuse; final applications again12000, unapplied parent1000. |
| P2 B receipt | Receive4000, apply3000 to B's remaining invoice; B fully paid and B credit1000 available. |
| Discoverable states | Parent invoice due1000; A due2000; B due0. P1 applied12000/unapplied1000; P2 applied3000/unapplied1000. Per-party AR parent0/A2000/B=-1000; family AR1000. |
| Independent net ledger increment | Dr new bank17000 + Dr AR net1000; Cr new income18000. Tax/other old accounts0 change. Total invoice due3000 minus unapplied payment credit2000 = net AR1000. New profit/equity effect18000; assets18000. Original/reversal/replacement of P1 nets to13000, not25500 or38000. |

Add a distinct cancelled invoice1000/payment1000 branch with explicit unapply, invoice void, payment void; its complete net effect is exactly zero and history remains. Thus final additions include four invoices (three active, one voided), three payments (two active, one voided), and two active payment-credit components usable for continuation. Financial receipt/invoice original net counts, dates, gross posting throughput and immutable allocation/history counts are independently enumerated from this fixed command sequence, not copied from implementation output. Permanent retry uses ORIGINAL request even after P1 correction; no second receipt. Final read-only demo pages must visibly offer partial, fully paid and reusable-owned-credit journeys. A test may copy the root to continue examples without mutating the delivered seed.

Fix the exact append length in the reviewed seed manifest before adjusting command/count/head expectations; independently derive each storage/history oracle and preserve the old full295/201 prefix. Upgrade/rehearsal verifies every old raw row/column/object/attachment except declared migration extensions and scoped principal last_seen_at heartbeat, captures each new request/key/receipt/audit delta, then repeats keys without new effects. Existing account aggregate balances may change ONLY by the specified new-ledger increment; no broad account/table exemption. Active original records are never rewritten. Parent owns actual publication separately.

Docs: parent amends blueprint10.4 AND owning fixture Shared checks to §7's exact tie oracle before implementation starts. On closure publish complete customer-payments owning contract, explicit permanent payment/applied-invoice key extension to6.5, service-sales corrections/output, architecture storage/authority/registry, generated schemas, calculator and agent guide. Include runnable CLI/HTTP/MCP↔GUI journeys with actual Row9 transport, recovered original-request examples, active demo incremental oracles and the required follow-up control inventory. Parent has already updated W9/W10 source evidence/inventory to the accepted F15 two-path reading; no duplicate edit is needed. Preserve parent intention/goal/publication ownership; no whole-form/full-goal completion claim at receipt-core closure.

Coherent internal implementation gates (all remain Row22, not separate completion claims): A preserving schema/typed models, complete-selection preparation and one-receipt/current balances with independent financial/migration review; B apply/unapply/corrections/restatement/permanent request recovery and authenticated-baseline/composite-authority review; C complete GUI/preferences/evidence/print/query/active seeds/docs; D actual Row9 MCP plus CLI/HTTP/GUI cooperating journeys and independent full artifact review. Core work may proceed while Row9 finishes, but Row22 cannot close without D. Accounting paths may freeze at a reviewable SHA while explicitly owned UI work continues; no overlapping codewriter. Complete plan PASS and parent tie-oracle amendments precede code. Closing this bounded receipt/application increment does not close the comprehensive goal or retire required dependent controls.

## 13. Concrete choices and remaining risk

This proposal chooses family new-cash derivation, fixed exact-party credit thereafter, payer residual, one AR, recursive new-money subtree, explicit unapply-before-void, payer-only amount delta, full source restatement on receipt correction, durable logical invoice ordinals, permanent company-wide operation keys plus unchanged optional30-day transport cache, and effective-date/current-knowledge reads. No unresolved accounting fork is delegated to implementation. The independently reviewed plan may revise these choices before code.

Material review focus: audited no-financial-effect executions; authenticated baseline reconstruction and related-header fan-out at403 applications; complete selection manifests and non-self-invalidating preparation pages; composite audit/evidence disclosure; effective-date capacities for backdated applications; source attribution through payment corrections. These are specified decisions needing proof, not claims that the features exist. If a real fragmentation dead end appears, surface its counterexample before a silent exception. This is a substantial multi-increment module: flag a long loop when a gate exceeds its agreed work window, continue independent scoped work, and do not call the comprehensive goal complete at module closure.

## 14. Pass1 disposition and freeze

Revision2 incorporates the parent's dispositions against `notes/reviews/payment-plan-pass1.md`. This is ready for independent re-review, not a self-awarded PASS. Only this /tmp proposal was edited; parent has completed the owning F14/F15 source amendments. No payment implementation is authorized until plan PASS.

| Finding | Disposition in this revision |
|---|---|
| F1 | Accept: actual Row9 MCP journey is a Row22 closure gate; core may proceed before it. |
| F2 | Accept: every first persisted operation, including financial no-op, has its own audit event/provenance; financial state unchanged. |
| F3 | Accept: versioned nonposting selection manifests span page/chunk limits; one atomic receipt derives all job owners.201/403 and alternating page/write witnesses; no remittance split or raised business cap. |
| F4 | Accept: all established text/number/date/bool/choice kinds, defaults/required/clear/preserve/history. Verified actual CustomFieldKind. |
| F5 | Accept: exact flat discriminated context modes, tagged selection source, query names/defaults/order and company-strategy behavior fixed. |
| F6 | Already closed: error catalog retained and extended for the explicit draft contract. |
| F7 | Accept: bounded signed guard contains authenticated audit baseline; reconstruct/verify owned baseline, attribute actual event field/actor/age, paginate comparisons, report unknown safely. |
| F8 | Accept: one composite rule for all detail/audit snapshots/activity/evidence; mixed protected events deny/filter as a whole, no generic-audit loophole. |
| F9 | Accept: authorized operation show/items recover executable original command/input/omissions/reason/versions and immutable manifest. |
| F10 | Accept: applied-invoice updates require permanent operation key; original correction recovers after generic expiry and later state changes. |
| F11 | Accept: shared exact-ID application show/history and browser links specified. |
| F12 | Accept: explicit posted/voided action matrix; update never revives a voided payment. |
| F13 | Accept: all existing nonstock, amount and progress profiles settle from stored net/tax integers. |
| F14 | Accept: parent amended blueprint and owning fixture to one logical tie rule with hand-fixed oracles and residue; no historical payments to migrate. |
| F15 | Source adjudicated: printed348 expressly supports blank selection-derived amount; printed663 supports entered cash/per-invoice calculation. Implement BOTH, preserve entered amount, no extra acceptance click. Parent corrected inventory/evidence. |
| F16 | Accept: typed payment_method plus date filters, before pagination, and browser lookup control. |
| F17 | Accept omissions in ordinary draft/save controls; implement them. Dispute requiring every later document/provider in this increment: approved goal explicitly sequences those dependencies. Control inventory assigns required owning follow-ups, preserves full-goal obligations, and forbids full-form parity claims at core closure. No user reapproval requested. |
| F18 | Accept: active partial/paid/owned-credit demo states with hand-derived17000 cash/1000 AR/18000 income incremental oracle; old records/prefix preserved, plus separate zero-net cancellation branch. |
| F19 | Accept observation: future separately authorized deletion preserves immutable ownership/history/evidence/keys, handles dependencies/date locks, never duplicates reversal or revives through retry. |

Long-loop notice: the revision exceeded the initial15-minute estimate because complete-remittance preparation and authenticated comparison baselines required concrete command/storage/continuation contracts, plus the original-source adjudication. No scope was silently dropped. Those decisions are now written; remaining work is independent plan review and parent disposition, not unfinished drafting or production changes.
